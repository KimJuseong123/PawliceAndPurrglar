# 변경 기록

이 프로젝트는 의미 있는 설계, 기능, 문서 변경을 기록한다.

## Unreleased

### Added
- **가방 칸에 마우스를 올리면 그 칸이 무엇인지 말한다**
  (`InventoryTooltipView`). 1~4번 칸의 사용 아이템은 **효과도 한 줄로** 알려준다 —
  "던진다. 맞은 사람이 1.2초 기절한다."
  - 칸 안이 아니라 옆에 뜬다. 70px 칸에 아이콘·개수·이름을 함께 넣는 것은 이미
    한 번 해봤고, **세 가지를 말하는 칸은 아무것도 말하지 않는다**. 칸 안의 이름
    라벨을 비워 둔 이유가 그것이라서, 툴팁 문구는 `ItemName`을 재사용하지 않고
    별도 필드(`TooltipTitle`/`TooltipBody`)로 보낸다. 아니면 창에 이름을 띄우는
    부수효과로 칸에 이름이 다시 박힌다
  - 효과 문장은 `ThrowableCatalog.GetEffectSummary`가 **상수로 조립**한다. 손으로
    적은 "3초"가 `GlueHoldSeconds`보다 오래 살아남으면, 플레이어는 화면이 보여준
    숫자로 계획을 세운 뒤 배신당한다
  - 보물에는 효과 줄이 없다. 가방 안에서는 아무 일도 하지 않고, 값은 너구리의
    장부에 있다 — 돈을 판단하는 자리가 거기다
  - 프리팹에 굽지 않고 런타임에 만든다. 생성한 스프라이트는 에셋이 아니라서
    에디터가 넣으면 **빈 참조가 되고 흰 사각형이 뜬다** (`ISSUE-050`). 너구리
    장부와 같은 방식이고, 덕분에 리빌드 메뉴 없이 동작한다
  - 창은 레이캐스트를 받지 않는다. 커서 밑에 뜨면서 클릭을 받으면 자기를 띄운
    칸에서 포인터를 빼앗아 **프레임 단위로 깜빡인다**
- **화면 왼쪽 위에 동물에게 할 말을 한글 표로 띄운다**
  (`CompanionVoiceCommandTableView`). 역할별로 다르게 나온다 — 도둑에게 개의
  명령을 보여주면 "짖어"를 시도했다가 침묵만 얻고 **이유를 알 방법이 없다.**
  못 알아들은 말과 남의 동물 명령은 똑같이 실패한다
  - 문구는 서버의 `exact-command-matcher`가 **모델 없이** 맞히는 어간이다. 즉 표에
    적힌 줄을 그대로 읽으면 반드시 해석된다
  - 한글 이름과 예시 문구는 `CompanionCommandCatalog`에 id 옆에 둔다. 화면과
    매처가 갈라지지 않게

### Changed
- **내가 조종하는 캐릭터의 위치 보간을 90ms에서 30ms로 줄였다.** 보간은 상대와
  동물에게는 순수한 이득이지만(아무도 자기 키 입력과 비교하지 않는다) **내 몸에는
  왕복 지연 위에 얹히는 추가 지연**이다. 역할로 판단한다 — 호스트는 두 캐릭터를 다
  소유하므로 `IsOwner`는 틀린 질문이다
  - **왕복 자체는 그대로다.** 접속한 쪽의 입력이 호스트에 가서 시뮬레이션되고 결과가
    돌아오는 것은 권위 구조이고, 그것을 없애는 것은 로컬 예측이다. 이 변경이 아니다
- **NGO 틱을 30에서 60으로 올렸다.** 틱은 NGO가 RPC와 변수 델타를 내보내는
  시점이므로, 30에서는 접속한 쪽의 키 입력이 나가는 데 최대 33ms, 호스트의 답이
  돌아오는 데 다시 최대 33ms를 기다린다. 네트워크와 무관하게 평균 30ms 남짓이
  얹히고 **그 비용은 접속한 쪽만 낸다** — 보고된 비대칭이 정확히 이것이다
  - **전송을 웹소켓으로 바꾸는 문제가 아니다.** Relay는 이미 wss로 돈다
    (`RelaySessionService`가 `UseWebSockets`와 `isWebSocket`을 함께 켠다).
    남은 지연은 구조적이다 — 접속한 쪽의 캐릭터는 호스트만 움직이고 결과가
    복제되므로 입력 한 번이 왕복 한 번이다. 그것을 없애는 것은 로컬 예측이고,
    이 변경이 아니다
  - 2인이므로 갱신 빈도가 두 배가 되어도 초당 몇 KB다
- **`Ctrl+1`~`4` 동물 명령을 없앴다.** 마이크가 없던 동안의 대역이었다. 둘 다 두면
  화면의 표가 둘 중 하나만 설명하거나 한쪽에 대해 틀리게 된다
  - 왼쪽 위의 `ANIMAL COMMANDS` 패널(`CTRL + 1..4`를 나열하던 것)도 함께 지웠다.
    없어진 조작을 설명하는 표였다
  - `SubmitCompanionCommandRpc`와 `TryIssue`는 그대로다 — 음성이 그 길로 간다
- **배포를 Caddy에서 nginx + Certbot으로 옮기고 마무리했다.** `upload.ps1`,
  `docs/21`, `CLAUDE.md`, `09_TASK_BACKLOG`에 남아 있던 Caddy 참조를 정리했다
  - **인증서는 스크립트가 발급하지 않는다.** 있는 것을 읽고, 없으면 멈추고 발급
    명령을 알려준다. Let's Encrypt에 발급 횟수 제한이 있어서, 재실행마다 멀쩡한
    인증서를 버리고 다시 받는 건 마감 직전에 최악이다
  - Certbot은 갱신 장치를 남기지 않는다. 90일 뒤 조용히 만료되고 증상은 "게임이 안
    열린다"이다. 하루 두 번 도는 `certbot-renew.timer`를 건다
  - 음성 서버가 `HOST`를 무시하고 있었다. 유닛 파일과 `.env`가 둘 다 루프백이라고
    적어 두고 실제로는 모든 인터페이스에서 듣고 있었다 — **적어만 둔 보안은 보안이
    아니다**

### Fixed
- **동물 명령표·상태 배너·먹물 화면이 빌드된 경기에 존재한 적이 없다**
  (`ISSUE-077`). `RuntimeInitializeOnLoadMethod`는 **실행당 한 번, 첫 씬 뒤에**
  불리고 빌드의 첫 씬은 로비다. 명령표는 거기서 걸러져 다시 안 불렸고, 나머지 둘은
  로비 씬 소속으로 만들어져 경기 로드가 지웠다. 이제 `MatchSceneInstaller`가 **경기
  씬이 로드될 때마다** 설치한다
  - 세 화면의 기존 테스트는 컴포넌트를 손으로 붙여 놓고 그림을 검사한다 — 설치
    코드를 지나가지 않아서 전부 통과했다. 새 테스트는 씬을 로드하고 **존재**를 본다
- **고양이가 명령을 이해해도 절대 따를 수 없었다** (`ISSUE-078`). 이해도 점수에
  복종심을 곱하고 같은 복종심으로 무시 굴림까지 해서, 출하된 설정에서 고양이의 최대
  점수 0.42가 문턱 0.78을 **영구히** 넘지 못했다. 완벽하게 들은 명령도 수행되지
  않는다. 이해와 의지를 분리했다 — 수치는 그대로다
- **클라이언트의 결과창이 비어 있었다** (`ISSUE-079`). 판정이 경기 씬 안의 고리 3개를
  거쳐 저장되는데, 그 씬은 호스트의 전환이 지우는 중이다. 이제 메시지를 받은 자리에서
  정적 저장소에 바로 적는다
- **실내에서 매 프레임 씬 전체를 훑었다** (`ISSUE-080`). 실내 보물 집계가
  `FindObjectsByType<HouseInterior>()`를 매 프레임 불렀다 — 방을 가장 가까이 보고 있는
  순간에 얹히는 비용이다. 캐시했다
- **박스·쓰레기통에 숨으면 캡슐이 땅속에 파묻혔다** (`ISSUE-076`). 숨는 자리의
  좌표를 `transform.position`에 그대로 넣고 있었는데, 그 좌표는 **발이 놓일
  바닥**이고 transform은 **캡슐의 가운데**다. 실측: 발바닥이 박스에서 −1.05m,
  쓰레기통에서 −0.69m
  - **평소에는 보이지 않는다.** 다음 프레임에 `CharacterController`가 겹침을
    위로 밀어내 세워 준다(피벗 1.08, `grounded`). 그래서 "한 프레임 뒤에 확인하는"
    기존 테스트는 계속 통과했다. 그 구제는 **컨트롤러가 돌고 있고 캡슐이 아직 땅과
    겹쳐 있을 때만** 일어난다 — 둘 중 하나만 어긋나면 플레이어는 그냥 세상 밖이다
  - 같은 산수를 세 번째로 틀렸으므로(`ISSUE-040`, `ISSUE-044`) 직접 쓰지 않고
    현관문이 쓰는 `InteriorTravel.Place`에 맡긴다 — 발 정렬·캡슐 끄기·
    `SyncTransforms`·낙하속도 초기화가 한 곳에 있다
  - **숨는 동안 캡슐을 끈다.** "숨으면 움직이지 않는다"가 입력에만 참이었다 —
    `CanMove`는 방향을 막고 중력은 막지 않아서, 숨어 있는 매 프레임 중력이 쌓이고
    `CharacterController.Move`가 불렸다. 덤으로 길에 서 있던 안 보이는 몸도 없어진다
  - 자리 좌표가 바닥보다 낮으면 **들어올 때 서 있던 바닥까지만** 내려간다.
    1.4m 안에 있어야 상호작용이 되므로 그 높이는 확실한 바닥이다
- **접속한 쪽의 음성 명령이 한 줄도 남기지 않고 사라졌다** (`ISSUE-075`).
  `CompanionVoiceCommandBridge.HandleBackendEvent`의 첫 줄 `if (!IsServer) return;`이
  접속한 쪽의 백엔드 응답을 그 자리에서 버렸다. 굴림과 실행이 호스트의 일인 것은
  맞지만 **버릴 것이 아니라 넘길 것**이었다
  - `NetworkPlayerLink.SubmitVoiceCommandResultRpc`로 호스트에 넘긴다. 오디오는
    여전히 NGO를 타지 않는다 — 넘어가는 것은 받아쓰기와 분류 결과뿐이다
  - 접속한 쪽의 `HandleNetworkEvent`가 **주석만 있는 빈 메서드**였다. 그래서 결과가
    돌아와도 상태 기계가 `Transcribing`에서 나오지 못했고, 화면은
    "음성 명령 처리 중"에 멈추고 쿨타임이 시작되지 않아 키가 죽어 보였다
  - 굴림은 호스트에서 **한 번만** 돈다. WebGL에서는 같은 명령이 소켓과 이 RPC 두
    경로로 닿으므로 `commandId`로 거른다
  - **모든 폐기 지점이 이제 `GameLogger`로 말한다.** 버릴 때 아무 말이 없던 것이
    증상을 마이크·서버·모델 어느 쪽으로도 읽히게 만들었다
  - `VoiceCommandInput`에 25초 마감. 답이 사라져도 키가 돌아온다
- **동물 명령 직후의 큰 멈춤** — `CompanionCommandResolver`의 지붕 찾기가
  `FindObjectsByType<Transform>()`으로 씬의 모든 트랜스폼을 훑고 이름마다
  `Contains`를 돌렸다. 고양이에게 오르라고 할 때마다 **호스트가 멈추고**, 명령을 내린
  사람은 반대편에서 복제가 끊긴 세상을 본다. 지붕은 움직이지 않으므로 한 번만 훑는다
- **HUD가 매 프레임 `FindFirstObjectByType<LocalPlayerRoleSelector>()`를 불렀다.**
  `ResolveRole()`이 한 프레임에 여러 번 불리고 그때마다 씬 전체를 훑었다
- **실내에서 도둑이 느려졌다 — 가방이 비어 있어도.** 실내 진입 시 캡슐 반지름을
  0.45에서 0.24로 줄이면서 **접촉 스킨 0.08은 그대로 뒀다.** 반지름의 3분의 1짜리
  스킨은 `CharacterController`가 실제로 닿지 않은 접촉을 해소하게 만들고, 벽과
  문틀에 걸린다. 스킨을 반지름의 10%로 함께 줄인다 (Unity 권고치)
  - 이동 배율은 그동안 내내 1.0이었다. **적재 페널티가 아니다**
- **상대가 투척을 당기는 소리와 상대 동물이 발견한 소리가 내 화면에서 났다**
  (`ISSUE-074`). 문 소리와 같은 원인인데 이쪽은 소리가 아니라 **정보 누출**이다 —
  당기는 시간이 곧 피할 수 있는 창이므로, 그 소리를 듣는 것이 대응의 전부다
  - 동물의 주인은 종류가 아니라 `CompanionAgent.Owner`로 묻는다. 개=경찰은 지금
    사실이지만 **규칙이 아니라 현재 배역에 대한 사실**이고, 주인이 바뀌면 소리가
    조용히 반대편 화면으로 간다
  - `Stunned`(상대가 맞은 소리)는 그대로 뒀다. 맞힌 쪽 피드백이 던지기 보상의
    대부분이고, 별을 양쪽 화면에 그리는 것과 같은 이유다

- **건물에 들어갈 때 문 소리가 두 번 났다** (`ISSUE-074`). `ISSUE-072`의 너구리
  인사·점프와 **정확히 같은 모양이고 세 번째다** — 두 역할 오브젝트는 양쪽 기계에 다
  있는데 관찰자가 그 전부에 하나의 핸들러를 구독시켰다. 상대가 어디서 문을 열어도
  이쪽에서 났고, 소리는 2D라 거리 감쇠가 없다
  - 세 번 같은 실수를 했으므로 묻는 방식을 하나로 모았다 —
    `LocalPlayerRoleSelector.TryResolveLocalRole`. 답이 없으면 `false`를 돌려준다.
    기본값 `Police`는 예전 방식이고 절반은 틀렸다

- **보석상에서 훔치면 경보가 맵 아래쪽 빈 들판을 가리켰다** (`ISSUE-073`). 실내는
  별도 씬이 아니라 **같은 씬 안에서 멀리 떨어뜨려 놓은 방**이라, 진열대 좌표는
  거리에서 걸어갈 수 없는 자리다. `LootAlarm.Raise`가 좌표를 먼저 도시로 번역한다 —
  방 안이면 그 건물의 현관 앞으로
  - **아무것도 로그를 남기지 않았다.** 경보는 울리고 신호는 10초를 채우고 화살표는
    거리에 따라 크기가 변했다. 전부 동작하면서 아무 데도 아닌 곳을 가리켰다
- **끈끈이에 붙었을 때 머리 위에 아무것도 없었다** (`ISSUE-073`). 스턴 사유를 나눌
  때 끈끈이가 둘 사이로 빠졌다. 이제 돌에 맞았을 때와 같은 별이 뜬다 — 예외는 자기
  연출(회전)이 있는 바나나 하나다
- **공중에서 스페이스바를 눌러도 점프 소리가 났다** (`ISSUE-073`). 거절된 누름까지
  소리를 내면 게임이 하지 않은 일을 했다고 말하는 것이다. 로컬은 `TryJump()`의
  반환값을, 클라이언트는 호스트가 복제하는 체공 플래그를 쓴다

### Changed
- **시끄러운 효과음 11종을 0.8로 낮췄다.** 귀가 아니라 실측으로 골랐다 — 새 메뉴
  `Report Sound Levels`가 뱅크의 클립을 전부 디코드해 RMS를 찍는다. 피크는 쓸모가
  없다(대부분 1.0이다 — 노멀라이즈가 그렇게 만든다). 측정값에 뚜렷한 단절이 있어
  0.47~0.21의 11개가 그 위였고, 그 아래는 0.16부터다
  - 센서등이 여전히 가장 크다(중앙값의 3배였다). 하나만 3분의 1로 내리는 것은
    고치는 게 아니라 믹스 결정이라 귀로 판단할 일로 남겼다
  - `VoiceRecognizeFail`만 0.6이다. `CommandFailed`와 **같은 녹음**인데 그쪽이 이미
    0.6이었다 — 한 파일이 호출 경로에 따라 두 음량으로 나는 믹스는 설명할 수 없다

- **경찰이 너구리 암시장에서 아무것도 살 수 없었다** (`ISSUE-072`). 같은 날 슈퍼마켓
  앞 보급 카운터 3개를 지운 것의 회귀다 — `PoliceSupplyCounter.TryInteract`가
  진열창이자 금전등록기여서, 진열은 폴백으로 계속 세 가지를 보여주고 사는 쪽만
  죽었다. 매번 "지금은 살 수 없어요"라고 답해 **골드가 부족한 것처럼 보였다**
  - 금전등록기를 `PoliceSupplyCatalogue`로 옮겼다. GameObject가 필요 없다
  - 구매 효과음도 카운터마다 구독하고 있어 함께 조용해져 있었다. 이제 카탈로그의
    정적 이벤트를 듣는다
- **너구리 인사 소리가 두 번 들렸다** (`ISSUE-072`). 이 게임의 소리는 전부 2D라
  거리 감쇠가 없는데, 인사는 *아무* 플레이어나 반경에 들어오면 났다 — 도둑이
  암시장에 가면 경찰 화면에서도 최대 음량으로 났다. 뚜껑과 손짓은 누가 와도
  반응하고, 소리만 이 기계의 역할에 붙는다

### Changed
- **점프 소리는 이제 스페이스바를 눌렀을 때만 나고, 음량이 0.6이다** (`ISSUE-072`).
  예전에는 `IsAirborne`가 서는 프레임이었는데 **땅에서 떨어지는 것과 점프하는 것은
  다른 사건이다** — 연석, 공원 계단, 경사면이 전부 세운다. 호스트가 두 캐릭터를 다
  시뮬레이션하므로 경찰은 도둑의 연석 소리까지 듣고 있었다

### Added
- **브라우저에서 초대코드로 만난다.** 로비의 IP·포트 칸을 없애고 **방 만들기 /
  방 입장**으로 바꿨다. 방을 만들면 6글자가 나오고, **코드 복사**로 클립보드에
  담아 상대에게 보내면 상대는 코드칸에 붙여넣고 방 입장을 누른다. 같은 공유기가
  아니어도 되고, 공인 IP도 포트 포워딩도 필요 없다.
  - 코드는 **Unity Relay의 join code 그 자체**다. 할당이 Unity 서버에 먼저 생기므로
    우리가 코드를 따로 발급하면 둘을 잇는 표를 어딘가에 들고 있어야 한다
  - `InviteCode`가 붙여넣기를 정리한다 — 앞뒤 공백, 하이픈, 줄바꿈, **보이지 않는
    제로폭 공백**까지. 대소문자는 입력하는 순간 접는다(폴링이 아니라
    `onValueChanged`에서 — 150ms 안에 방 입장을 누르면 접히기 전 문자열이 나간다)
  - 거절 사유를 나눠서 말한다. "6글자입니다"와 "영문자와 숫자뿐입니다"는 각각
    글자를 빠뜨린 것과 URL을 붙여넣은 것이고, 플레이어가 할 일이 다르다
  - 클립보드는 `.jslib`로 간다. `GUIUtility.systemCopyBuffer`는 WebGL에서
    **플레이어 내부 버퍼에 쓰고 페이지의 클립보드에는 닿지 않는다**
- **커스텀 WebGL 템플릿.** 기본 템플릿은 캔버스를 960×600 상자에 가둔다. 이 템플릿은
  창을 채우고, 진행률을 보여주고, **로딩에 실패하면 그 이유를 화면에 쓴다** — 기본
  템플릿은 빈 페이지만 남긴다.
- **`Build WebGL Release` 메뉴**와 `deploy/` 일습 (Caddyfile, systemd 유닛,
  `setup-ec2.sh`, `upload.ps1`, `voice.env.example`).

### Changed
- **경기 연결이 Unity Relay를 지나간다.** 선택이 아니라 제약이다 — Unity Transport는
  **Relay 프로토콜이 아니면 WebGL을 서버로 쓰는 것을 거부한다**(`UnityTransport.cs`가
  그 자리에서 throw 한다). 브라우저는 듣는 소켓을 열 수 없으므로 "한 명이 호스트하고
  IP를 불러준다"는 모델은 브라우저로 가는 순간 성립하지 않는다.
  - 전송 종류는 어디서나 `wss` 하나다. 데스크톱만 `dtls`로 두면 플레이테스트가 검증한
    전송과 제출본이 쓰는 전송이 달라진다
  - 직접 IP 경로(`TryStartHost`/`TryJoin`)는 남겼다. 인터넷도 Unity 계정도 없이 도는
    유일한 회귀 경로이고 `-netJoinMode api`가 그대로 쓴다
  - **EC2에는 게임 서버가 없다.** 정적 파일과 음성 API만 있고 열리는 포트는 80·443·22
    뿐이다. t3.micro로 Unity 헤드리스 서버를 여러 개 돌릴 수 없다는 사양 문제도,
    리눅스 빌드 모듈이 없다는 문제도 함께 사라졌다
- **WebGL 빌드가 자기 도메인으로 음성 API를 부른다.** `VoiceBackendAddress`가
  `Application.absoluteURL`에서 오리진을 뽑는다. 설정에 박힌 `http://localhost:3000`은
  https 페이지에서 **브라우저가 통째로 차단한다**
- 압축은 Gzip에 **Decompression Fallback을 켰다.** 산출물이 `.unityweb`이 되고
  브라우저가 JS로 푼다 — 서버에 `Content-Encoding`을 맞출 필요가 없어진다. 헤더 한 줄
  때문에 흰 화면이 뜨는 것이 WebGL 배포에서 가장 흔한 실패이고, 로딩 1~2초로 그 실패를
  사는 편이 낫다

### Fixed
- **배치 빌드가 매번 저장소를 더럽히고 있었다.** `BuildStamp.Dispose()`가
  `PlayerSettings.bundleVersion`을 메모리에서만 되돌리고 저장하지 않았다. 배치 모드는
  그 직후 종료하므로 복원이 디스크에 닿은 적이 없고, 빌드할 때마다
  `bundleVersion: 1.0+<커밋>-dirty`가 작업 트리에 남았다 — **"도장은 저장소를 더럽히지
  않는다"의 정확히 반대**다. 에디터 메뉴로 빌드하면 프로세스가 살아 있어 나중의 저장이
  실어 나르기 때문에 눈에 띄지 않았다
- **로비의 마이크 확인이 브라우저에서 항상 "마이크 없음"이었다.**
  `UnityEngine.Microphone`은 WebGL에 구현이 없어 장치 목록이 늘 비어 있는데, 정작
  녹음은 `BrowserVoiceCaptureProvider`(MediaRecorder)로 멀쩡히 된다. WebGL에서는
  `getUserMedia`로 실제 녹음을 해 보고 판정한다. https 아님·권한 거부·미지원을
  각각 다른 문장으로 말한다
- **LAN 방 목록이 Relay 방을 광고하고 있었을 것이다.** `Mode == Host`만 보고 있어서,
  Relay로 연 방도 이 네트워크의 포트를 가리키는 광고를 뿌렸다. 상대 목록에는 **아무것도
  듣고 있지 않은 포트를 가리키는 방**이 뜨고, 누르면 설명 없이 타임아웃된다.
  초대코드가 있는 세션은 광고하지 않는다
- **WebGL에서 터질 코드를 컴파일에서 뺐다.** `UdpClient`(방 발견)와
  `Dns.GetHostAddresses`(내 주소)는 **컴파일이 아니라 첫 호출에서** 터진다.
  `ApplicationQuitButton`은 브라우저가 탭을 닫게 해 주지 않으므로 스스로 숨는다 —
  아무 일도 하지 않는 버튼은 이 프로젝트가 이미 세 번 찾은 실패다

### Fixed
- **"안 움직인다"가 이유를 말하도록 했다.** `NetworkInputBridge`는 역할이 없거나
  자기 역할의 `NetworkPlayerLink`가 스폰되지 않으면 **입력을 아예 보내지 않고
  조용히 돌아갔다.** 플레이어가 겪는 것은 움직이지 않는 캐릭터인데 로그에는 아무것도
  없어서, 두 원인이 화면에서 구분되지 않았다.
  - 역할 없음 → 로비의 `CommitRolesRpc`가 도착하지 않은 것 (`Committed local role`
    로그가 있는지 보라고 함께 적는다)
  - 링크 미스폰 → 두 기계의 빌드가 달라 in-scene 오브젝트가 스폰되지 않은 것
  - **3초 이상 계속될 때만** 말한다. 세션을 나가면 역할이 잠깐 비고 씬 로드 직후에는
    링크가 한 박자 늦게 스폰되는데, 첫 프레임에 외치면 정상적인 전환이 오류가 된다 —
    실제로 `LobbyReentryPlayModeTests`를 깨뜨렸다

### Fixed
- **커밋이 같아도 빌드가 다를 수 있는데 지문이 "같다"고 답하고 있었다.**
  `PlaytestBuild`의 도장은 `git rev-parse --short HEAD`뿐이라 **작업 트리가 더러운지
  보지 않았다.** 재생성된 `Game.unity`를 커밋하지 않은 PC의 빌드와, 같은 커밋을
  클론해 만든 다른 PC의 빌드가 **둘 다 `1.0+5e5c933`으로 찍혔다** — 그 하나를 잡으라고
  만든 검사가 통과를 보고했고, 플레이어가 겪은 것은 "클라이언트가 안 움직인다"였다.
  - `Assets`·`ProjectSettings`·`Packages`에 미커밋 변경이 있으면 도장에 `-dirty`가
    붙는다. 범위를 좁힌 것은 문서나 `server/`까지 세면 거의 모든 빌드가 dirty가 되어
    **늘 켜진 경고**가 되기 때문이다
  - 빌드할 때 그 사실을 경고로도 남긴다 — 다른 기계는 그 변경을 클론할 수 없다
- **`Assets/_Project/ArtSource.meta`가 커밋에서 빠져 있었다.** 폴더 안쪽만 add 하고
  폴더 자신의 meta를 빠뜨렸다. 클론한 기계에서 Unity가 다른 GUID로 다시 만든다.

### Changed
- **효과음 37개 전부 CC0(freesound)로 확인해 기록했다.** 2026-08-09에 들어온 27종
  (C-4~C-11의 11종, C-1~C-3의 16종)이 "미확인"으로 남아 있던 것을 채웠다.
  `docs/17`과 `THIRD_PARTY_NOTICES.md` **양쪽**을 갱신했고, `SUBMIT-005`에서 효과음
  항목이 빠졌다 — 남은 것은 BGM과 Tripo 증빙이다.
  - URL은 세 묶음 어디에도 남아 있지 않다. CC0라 라이선스 문제는 없지만, 교체하거나
    다시 찾을 때는 처음부터 찾아야 한다

### Changed
- **`sfx_case_unlock`을 14.2초에서 2.80초로, `sfx_fuse_burn`을 8.10초에서 2.50초로
  잘랐다.** 각각 진열장 여는 시간(`UnlockSeconds` 2.8초)과 도화선(`FireworkFuseSeconds`
  2.5초)에 맞췄다 — 후자는 이제 폭죽이 터지는 순간 정확히 멎는다.
  - **`sfx_case_unlock`은 앞에서 자르면 안 됐다.** 파일이 2초 가까이 무음으로
    시작하고 자물쇠 소리는 8.5초 뒤에 몰려 있었다. 앞 2.8초를 가져왔으면 거의 무음인
    클립이 나왔을 것이고, **뱅크에는 정상으로 들어가고 로그도 깨끗했을 것이다.**
    0.1초 단위 RMS로 에너지가 가장 큰 2.8초 창(8.50~11.30초, 전체의 61%)을 골랐다
  - 시작 5~10ms 페이드인, 끝 25ms 페이드아웃. 끝 표본은 정확히 0이다 — 파형을 그냥
    끊으면 딸깍 소리가 난다. 같은 이름으로 덮어썼고, 원본은 `git show ade15f1:` 로 꺼낸다

### Added
- **효과음 16종을 더 넣고 `23_SFX_ACQUISITION_SHEET`의 C-1~C-3을 닫았다.**
  `GameSoundId`가 24개에서 **43개**로 늘었다 — 고무닭, 도화선, 폭죽(가까이/멀리),
  투척 3종, 기절, 먹물, 함정 설치, 바나나, 센서, 미끼, 뒤지기, 유리 깨기, 열쇠 열기.
  - **`sfx_firework_bang`은 받은 파일이 아니라 만든 파일이다.** 폭죽이 두 파일로
    왔고 반드시 순서대로 나야 하는데, 오디오 계층이 `PlayOneShot` 하나뿐이라 같은
    프레임에 두 번 부르면 겹쳐 나온다. `FireworkBangMerge`가 48kHz로 리샘플해 잇고
    이음매에 5ms 크로스페이드를 넣어 **순서를 파일의 성질로 만들었다.** 원본 둘은
    `ArtSource/Audio/`에 남겨 다시 만들 수 있다
  - **`sfx_dog_alert`·`sfx_cat_alert`·`sfx_glue_stick` 3종은 코드만 완성했다.**
    파일을 넣으면 그대로 동작하고, 그때까지 뱅크 조립이 세 줄로 경고한다. 예순 줄을
    지운 것과 모순이 아니다 — 셋은 목록으로 읽히고 예순은 소음이다
- **함정 소리는 `NetworkItemCoordinator`에 놓았다.** `PlacedTrap.Triggered`가 당연해
  보이지만 `TickTraps`는 설계상 호스트 전용 스윕이라, 거기 걸면 **클라이언트는 자기가
  밟은 바나나 소리를 못 듣는다.** 양쪽이 모두 지나가는 `Spawn`(설치)과 `Clear`(발동)에
  걸었다.
  - 폭죽 굉음만 `ApplyBangLocally`에 있다. **거리를 아는 유일한 자리**라서 같은 폭발이
    한쪽에서는 가깝고 다른 쪽에서는 멀 수 있다 — 클립을 둘로 나눈 이유 전부다.
    `NoiseBoard.Heard`에 걸 수 없는 이유이기도 하다: 유리 깨는 소리도 그 게시판에
    보고되므로 **진열장을 깨면 폭죽 소리가 났을 것이다**
- **`Stunned`는 돌 명중에만 울린다.** 바나나와 끈끈이도 기절이고 각자 소리가 있어서,
  전부에 얹으면 한 사건에 소리가 둘이고 앞의 것이 이미 다 말했다. 기절이 아니라
  `StunCause.Impact`를 본다.

### Known
- **받은 클립 여섯 개가 자기 슬롯보다 길다.** 자르는 것은 되돌리기 어려워 그대로 넣고
  `docs/23`에 표로 적었다. `CaseKeyUnlock` 14.2초(여는 데 2.8초)와 `NoisePropFuse`
  8.1초(도화선 2.5초)가 특히 심하다 — 후자는 폭발 뒤까지 탄다.

### Added
- **보석상은 이제 유리를 깨거나 무엇을 훔쳐도 10초짜리 사이렌을 울린다.**
  예전에는 왕관 보석 하나(`RaisesAlarm`)에만 달려 있어서, 진열장을 부수고 그 옆의
  다른 물건을 집으면 가게 쪽에서는 **아무 일도 없었다.** 이제 `jewel-`로 시작하는
  물건 전부와 진열장 파손 자체가 사이렌을 낸다
  - 경찰 화면에는 센서등과 같은 모양의 와이파이 신호가 **보석상 방향으로** 10초간
    뜨고, 거리에 따라 크기가 줄어든다 (`LootAlarm.BeaconSource`,
    `SensorRadarPresenter`)
  - **사이렌 효과음 파일이 아직 없다** — `sfx_alarm_siren`
    (`docs/23_SFX_ACQUISITION_SHEET`)

### Changed
- **진열장 유리는 1.1초가 아니라 2초를 눌러야 깨진다.** 그리고 깨고 나면 안의
  다이아몬드 반지를 줍는 상호작용이 따로 뜬다 — 유리 자체에는 모델을 두지 않는다
- **남동쪽 호수 공원(Lake Garden)에 올라갈 수 있다.** 광장과 같은 방식으로 포장면을
  0.24~0.25m로 낮췄다 (`stepOffset` 0.35 아래). 화단과 바위는 0.43~0.98m로 남겨
  여전히 막는다
- **도시 한복판의 흰 상호작용 큐브(`Prototype Plaza Point`)와 경찰 보급 카운터 3개를
  치웠다.** 큐브는 누른 횟수를 세기만 하던 프로토타입 잔재이고 화면에서는 광장 위에
  뜬 유리판으로 보였다. 카운터는 너구리 상인이 같은 세 가지를 같은 값에 팔고 있어
  길 위의 큐브 세 개가 중복이었다 — `MerchantTradePresenter.ResolveStock`이
  카운터가 없으면 끈끈이 60 / 센서등 90 / 참치캔 40으로 폴백한다

### Added
- **효과음 11종을 넣고 `docs/23_SFX_ACQUISITION_SHEET`의 C-4~C-11을 닫았다.**
  `GameSoundId`가 10개에서 **24개**로 늘었고 전부 클립이 있다 — 문 여닫기와 실내
  출입(하나로 통일), 가방, 점프, 보급소 구매, 카운트다운, 역할 배정(경찰·도둑 별개),
  너구리, 음성 4종, 상대 접속·이탈.
  - **남은 60행은 기다리게 두지 않고 지웠다.** 파일 없는 ID는 뱅크를 조립할 때마다
    경고를 하나씩 낳고, 예순 개가 쌓이면 정작 봐야 할 경고가 묻힌다
  - 새 파일이 필요 없던 것들은 있는 것을 쓴다 — 실내 소품 줍기는 `sfx_loot_pickup`,
    음성 인식 실패는 `sfx_command_fail`, 고양이 전달은 `sfx_cat_meow`
  - `sfx_countdown_tick`은 한 박자가 아니라 **3초짜리 카운트다운 전체**라, 초마다가
    아니라 카운트 시작에 한 번만 낸다. 초마다 냈으면 같은 소리가 1초 간격으로 세 겹이 된다

### Fixed
- **로비와 결과 화면에서는 효과음이 한 번도 난 적이 없었다.**
  `GameSoundService`는 `GreyboxMapSetup`이 **`Game` 씬에만** 만든다. 그래서 로비의
  역할 배정, 상대 접속·이탈, 음성 모델 준비 완료는 낼 곳이 없어 요청이 조용히
  버려졌다 — **로그도 남지 않는다.** 클립이 없는 것과 서비스가 없는 것이 화면에서
  똑같이 보이므로 아무도 알아채지 못했다.
  - 뱅크를 `Settings/Audio/`에서 `Resources/Audio/`로 **옮겼다**(복사가 아니다 —
    두 벌은 갈라지고 갈라진 것을 아무도 모른다). 서비스가 없으면 이름으로 뱅크를
    찾아 `PersistentOneShotAudio`로 낸다
  - `Game.unity`의 참조는 GUID라서 옮겨도 끊기지 않았고 **씬을 재생성하지 않았다.**
    재생성은 in-scene `NetworkObject` 130개의 해시를 전부 바꿔 기존 빌드를 못 쓰게 만든다
- **실내 소품을 주우면 상인에게 판 소리가 났다.** 지갑이 늘어난 이유가 판매인지
  주머니인지 금액만으로는 구분되지 않아서, 둘 다 `LootSold`로 갔다. 방을 뒤지는 것은
  거래가 아니라 줍는 것이므로 `ThiefLootWallet.CashPocketed`로 갈라 `LootAcquired`를 낸다

### Changed
- **보물 판매가를 1/5로 내렸다 (일반 200→40, 고급 350→70, 희귀 500→100).**
  희귀 두 개가 곧 목표 1,000골드였다 — 획득 키 두 번과 상인까지 한 번의 왕복이
  경기 전체였고, 경찰이 요구받는 체포 3회(사이에 감옥 11초씩)는 일어날 시간이
  없었다 (`ISSUE-071`, `DEC-033`).
  - 등급 간 비율 1 : 1.75 : 2.5와 세 등급 구조는 그대로다. 목표 1,000골드도
    그대로 — 그 값은 2프로세스 회귀와 HUD 문자열과 몰수 비율의 기준이라 가격보다
    훨씬 많은 것에 묶여 있다
  - 실내 귀중품 20 → 5. 총액이 밀려서가 아니라, 뒤지기는 운반도 왕복도 위험도
    없으므로 **그 전부를 치르는 보물보다 확실히 적게** 벌어야 한다. 20을 그대로
    두면 40짜리 일반 보물의 절반을 공짜로 버는 셈이다
  - 결과: 맵 총액 22,150 → **4,430**, 슈퍼마켓 한 곳 2,750 → **550**(목표의 55%),
    가장 싼 집 1,250 → **250**(25%). 이제 어느 방도 혼자서는 목표를 넘지 못하고,
    도둑은 가방에 모아 최소 한 번은 마을을 가로질러야 한다
  - 경찰 경제는 손대지 않았다. 몰수가 누적 판매액의 **비율**이고 목표가 고정이므로,
    같은 진행률에서 경찰이 받는 금액은 가격과 무관하다
- **`PlayerInteractionSceneTests`에 위쪽 한계를 넣었다.** 지금까지는 "맵 총액 >
  목표"라는 아래쪽만 검사했다 — 도둑이 이길 수 **있는가**만 묻는다. 22,150 대
  1,000이라 아주 여유롭게 통과했고, 목표를 즉시 넘길 수 있다는 사실은 어느
  테스트도 보고 있지 않았다. 이제 **어느 한 방도 목표 이상을 들 수 없다.**

### Fixed
- **경찰에게 빈 상자·쓰레기통의 상호작용 안내가 떴고 누르면 아무 일도 없었다.**
  역할 검사가 `TryInteract`에만 있어서 **누른 뒤에** 거절했다 — 프롬프트를 정하는
  `IsAvailable`은 역할을 몰랐다. `IRoleAwareInteractable`을 만들어
  `InteractionResolver.IsValid`에서 묻는다. 프롬프트와 키 입력이 같은 경로를 지나므로
  한 곳으로 둘 다 막힌다.
  - `PlayerHidingSpot`: 경찰에게는 **누가 들어 있을 때만** 뜬다 (끌어내기는 끌어낼
    사람이 있을 때만 존재한다). 숨기는 도둑 전용
  - `LootHidingSpot`: 도둑 전용. 경찰이 상자에 할 일은 너구리 시장이고 그건 다른
    컴포넌트다
  - 라쿤 쓰레기통은 그대로다 — 다른 컴포넌트이고 두 역할 모두 볼일이 있다
- **끈끈이와 센서등을 설치하면 파란 상자만 놓였다.** `GetModelStem`이 둘 다 `null`
  이라 `PlacedTrapView`가 프리미티브를 그렸다. **아트는 `ArtSource`에 계속 있었고**,
  HUD 아이콘이 이미 쓰던 이름 그대로였다 (`catnip pouch`, `police lantern alarm`).
  - `trap_glue`·`trap_sensor_light`로 임포트하고 카탈로그·크기·Resources 프리팹까지
    연결했다
  - 센서등은 램프 때문에 **모델을 갖는 것 자체에서 제외**돼 있었다. 램프를 모델에
    붙이도록 바꿨다 — 그 제외가 다른 소품이 전부 아트를 받은 뒤에도 회색 원기둥이
    남아 있던 이유다
  - `PropsWithoutArtNameNoModel` 테스트는 **정확히 반대를 단정하고 있었다.** 쓴
    시점에는 맞았고 그대로 두면 둘을 영원히 회색으로 묶어 둔다. "설치 소품은 전부
    모델을 갖는다"로 뒤집었다
- **공원(분수 광장)에 올라갈 수 없었다.** 건물과 같은 취급을 받아 **8 × 2.8 × 8 통짜
  박스**가 씌워져 있었고 윗면이 2.78m — stepOffset 0.35로는 못 넘는 벽이다. 화면에는
  보이지 않는 벽으로만 보인다.
  - `walkOn` 모드를 만들어 **MeshCollider**로 바꿨다. 포장은 밟히고 분수·울타리는
    막는다
  - 높이는 **재서** 맞춘다. 처음엔 중앙값을 썼는데 8m 정사각형에서는 표본 대부분이
    화단에 떨어져 포장이 지면 **아래로** 내려갔다. 포장은 이 모델의 가장 낮은 면이라
    최솟값이 맞다. 실측: 포장 **0.25m**(넘어감), 울타리 0.74, 분수 1.71

### Added
- **집 안에서 나가는 곳에 표시가 생겼다** (`InteriorExitMarkerView`). 출구는 바닥의
  트리거일 뿐이고, 실내 벽은 안쪽에 문이 그려지지 않은 단일 스캔 메시라 **어느 벽으로
  들어왔는지 외우는 것**이 유일한 방법이었다.
  - 바닥 빨간 링 + 그 위에서 까딱이는 표식. 링은 "정확히 어디", 까딱임은 방 건너편에서
    "저기"를 답한다
  - 링 감기는 `NoisePingView`의 것을 그대로 가져왔다 — 이 저장소에서 **실제로 그려지는
    것이 확인된** 링이다. 기절 별이 존재 내내 안 보였던 것이 이 실수였다. 까딱이는
    쪽은 프리미티브라 감기를 틀릴 수가 없다


### Added
- **`NetworkSceneFingerprint`** — 두 기계가 다른 빌드일 때 한 줄로 말한다.
  `Game.unity`를 재생성하면 in-scene `NetworkObject` **130개의
  `GlobalObjectIdHash`가 전부 바뀐다.** 다른 커밋 빌드끼리 붙이면 NGO가
  오브젝트마다 `soft synchronization failure`를 찍고 `NullReferenceException`으로
  끝나는데, **어디에도 "빌드가 다르다"는 말이 없다.**
  - 플레이어가 겪는 것은 "안 움직인다"다. 실패한 오브젝트가 `Police Player`이고
    거기에 이동 입력이 지나가는 `NetworkPlayerLink`가 붙어 있다. **가방은 로컬
    UI라 멀쩡히 동작해서 이동 버그로 읽힌다**
  - `NetworkBehaviour`가 **아니다.** 그랬다면 자기가 보고하려는 바로 그 이유로
    스폰에 실패한다. 명명 메시지는 스폰된 오브젝트가 필요 없어서 이때도 도달한다
  - 처음엔 씬의 `GlobalObjectIdHash`를 직접 지문으로 쓰려 했는데 **컴파일이 안 된다**
    — 그 필드는 Netcode 패키지 `internal`이다. 커밋을 비교하는 쪽이 어차피 더 맞는
    질문이다
  - 도장이 없는 빌드(에디터·수동 빌드)는 **비교 불가라고 말한다.** 조용히 넘어가면
    통과와 구분되지 않는다
- **`PlaytestBuild`가 빌드마다 git 커밋을 박는다** (`PlayerSettings.bundleVersion`).
  빌드가 끝나면 되돌리므로 **저장소는 더럽혀지지 않는다.** Windows·WebGL·서버 3곳
  전부.


### Fixed
- **건물에 들어가면 캐릭터가 바닥에 허리까지 파묻혔다.** 내가 앞 커밋에서 만든
  회귀다. 메시 예산 패스가 충돌 사본(`*_col.fbx`)의 **법선을 벗겨냈는데**,
  `HouseInteriorSetup.MeasureFloorTop`이 바로 그 법선을 읽어 "위를 보는 면"을 골라
  방의 바닥 높이를 정한다. 법선이 없으니 `mesh.normals`가 빈 배열이 되고,
  `index < normals.Length` 루프가 **한 번도 돌지 않고**, 측정은 조용히 fallback —
  **모델의 밑면** — 을 돌려줬다. 그 자리에 진입 지점이 놓였다.
  - 예외도 경고도 없었다. 씬은 저장되고 생성기는 성공을 찍고 **512개 테스트가 전부
    통과했다.** 찾는 방법은 건물에 걸어 들어가는 것뿐이었다
  - 충돌 메시의 법선을 되살렸다. 렌더러는 파괴되지만 **법선은 렌더링용이 아니라
    측정용**이라 벗기면 안 된다 — `ResourceWeightPass`에 그렇게 적었다
- **`MeasureFloorTop`의 fallback이 더 이상 조용하지 않다.** 바닥을 못 재면
  `Debug.LogError`로 방 이름·충돌 메시 수·법선 없는 메시 수·측정 한계선·찾은 위쪽
  면 개수를 함께 찍는다. 경고가 아니라 오류인 이유는 그때 나오는 씬이 플레이할 수
  없는 씬이고, 2천 줄짜리 생성기 로그의 경고는 아무도 안 읽기 때문이다.
- **그 오류가 곧바로 별개의 결함을 하나 더 드러냈다.** 감옥(`interior_jail`)은
  법선이 멀쩡한데도 바닥을 못 재고 있었다 — 방 shell 경계가 **비어 있어서**
  (`size.y = 0`) 판정 한계선이 `y = 0`이 되고, 실제 바닥이 1.63에 있는 위쪽 면
  4,355개가 **전부 제외**됐다. 이건 내 변경 이전부터 있던 것이고, fallback이 조용한
  동안에는 "감옥에 바닥이 없다"로만 보였다. shell을 못 믿을 때는 충돌 지오메트리
  자체에서 경계를 재도록 고쳤다.
- **회귀 방지 테스트 추가** (`InteriorArrivalFloorTests`). 저장된 씬에서 모든 실내의
  진입 지점 아래로 레이캐스트해 바닥과의 높이차를 잰다. 실측 **28개 지점, 0.00~0.41m
  위**. 측정한 지점 수를 함께 단정하고 로그로 찍는다 — "통과했다"와 "실제로 뭔가를
  쟀다"는 다른 주장이고, 두 번째가 첫 번째를 값어치 있게 만든다.

### Added
- **`docs/23_SFX_ACQUISITION_SHEET.md`** — 효과음 94종 수집 작업표. `docs/16` C절의
  목록에 **파일 이름(`sfx_*`)·사용처(코드 이벤트)·freesound 검색어**를 채웠다.
  - 못 찾은 9종은 **검색어를 물건이 아니라 동작으로 다시 적었다** (`curtain open` →
    `fabric swoosh`), 그래도 안 되면 쓸 재사용 후보까지 적었다
  - 받는 순서를 명시했다: 루프 채널이 없으면 앰비언스 5종과 도화선은 **붙일 자리가
    없다**. C-1 → C-3 → C-2가 먼저다
  - 이미 만든 연출과 짝인 것을 표시했다 — 기절 별, 바나나 회전, 먹물 오버레이,
    끈끈이·센서등 배너. 소리가 나는 순간이 그 연출이 뜨는 순간과 같아야 한다
  - `IconHappy`/`IconConfused`가 기존 `CommandSucceeded`/`CommandFailed`와 **같은
    순간에 울린다**는 것을 짚었다. 그대로 두면 소리 두 개가 겹친다

### Changed
- **`.gitattributes`에 `*.flac`·`*.aiff`·`*.aif`를 LFS로 추가했다.** freesound에는
  FLAC이 흔한데 패턴이 없어서, 기존 `sfx_dog_bark.flac`은 실제로 **LFS를 우회해
  저장소 본문에 들어가 있다**(53KB). 지금은 작지만 94종이 같은 경로로 들어오면
  아니게 된다. 기존 파일은 이력을 다시 쓰지 않으면 못 옮기므로 그대로 두고, **앞으로
  받는 것**만 LFS로 간다.


### Changed
- **영문 제목을 `PawliceAndPurrglar`로 확정했다.** 한글 제목 `멍경찰과 냥도둑`은
  그대로다. 바꾼 것은 **사람이 제목으로 읽는 곳 전부**다:
  - `productName`·`companyName` — 창 제목과 브라우저 탭에 나오는 이름.
    저장 경로가 `%LOCALLOW%\PawliceAndPurrglar\PawliceAndPurrglar\`에서 `%LOCALLOW%\PawliceAndPurrglar\PawliceAndPurrglar\`로 **실제로 옮겨간 것을
    빌드해서 확인했다** (`net-lobby-host-result.json`이 새 경로에 떨어진다).
    `CLAUDE.md`·README의 프로브 경로를 전부 갱신했다. **기존 결과 JSON은 고아가 된다**
  - 에디터 메뉴 루트 `Paws & Loot/...` → `PawliceAndPurrglar/...` (81파일, 121곳).
    메뉴 이름을 적어둔 문서도 함께 갱신했다
  - README·`docs/`·`AGENTS.md`·`CLAUDE.md`·`THIRD_PARTY_NOTICES.md`의 제목 표기
  - "현재 제목은 임시 제목이며…" 문구 제거. 임시가 아니게 됐다
  - **공백을 뺐다.** 처음엔 `Pawlice and Purrglar`로 넣었는데 저장 경로에 공백이
    들어가 명령줄마다 따옴표가 필요했다. 붙여쓰기가 경로·패키지명·네임스페이스와
    같은 모양이 된다
  - 저장소 슬러그 `PawliceAndPurrglar` → `PawliceAndPurrglar` (문서 경로, npm 패키지명)
- **바꾸지 않은 것은 코드 식별자다**: 네임스페이스 `PawliceAndPurrglar.*`, 어셈블리
  `PawliceAndPurrglar.Runtime`, 빌드 산출물 `PawliceAndPurrglar.exe`.
  - 이유는 규모가 아니라 **씬이 그것을 직렬화하고 있어서**다. `Game.unity`·
    `Bootstrap.unity`·`HudCanvas.prefab`에 `PawliceAndPurrglar.Runtime`이 **730번**,
    컴포넌트마다 정규화된 타입 이름이 `m_EditorClassIdentifier`로 박혀 있다.
    어셈블리 이름을 바꾸면 그 문자열이 전부 낡고, 참조가 조용히 끊겨도 테스트가
    잡아 준다는 보장이 없다 — 이 저장소가 반복해서 당한 종류의 실패다
  - 판단이 필요한 작업이라 별도로 남긴다. 하려면 씬·프리팹 전부 재생성 + 2프로세스
    실기까지 묶어서 한 번에 해야 한다

### Removed
- **`Assets/CatCops/`가 아직 있다고 말하던 문서를 정리했다.** 폴더는 이번
  `CLEAN-001`에서 지웠는데 README·`docs/07`·`docs/13`·`prompts/`가 여전히 그 경로를
  현재형으로 가리키고 있었고, 저장소 루트를 `C:\Users\SSAFY\CatCops`로 적은 곳도
  네 군데 남아 있었다.
  - **이력 문서는 건드리지 않았다** — `CHANGELOG`·`11_AI_USAGE_LOG`·`14_DECISION_LOG`·
    `15_KNOWN_ISSUES`는 그때 무슨 일이 있었는지의 기록이고, 지금 사실과 다르다고 해서
    고쳐 쓰면 기록이 아니게 된다
  - `docs/13`의 "레거시 실험물 처리" 절은 계획이 아니라 **완료 보고**로 바꿨다


### Removed
- **TopDown Engine 의존을 전부 걷어냈다.** 확인부터 하면: 그 에셋은 저장소에 없고
  (`.gitignore`), git이 추적한 적도 없고, **이 개발 PC에도 설치돼 있지 않았다.**
  `CATCOPS_TOPDOWNENGINE`도 정의돼 있지 않았다. 즉 남아 있던 것은 전부 **한 번도
  해석된 적 없는 참조**였다.
  - `CharacterLocomotion.controller` — 클립 GUID **6개 전부 미해결**. 이 컨트롤러는
    TDE가 있던 기계에서 한 번 생성돼 커밋된 것이고, 그 뒤로 줄곧 빈 구멍 여섯 개였다
  - `LegacyControllerGuidTextRemainsUnchanged` — 그 여섯 구멍의 GUID를 **바뀌지 않도록
    지키는** 테스트였다
  - `AnimationClipSourceAudit` (466줄) + 테스트 — 존재 목적이 "TDE 의존을 보고하기"였다
  - `PlaceholderModelAudit`의 `Audit Placeholder Character Models` — TDE FBX 7개 경로를
    훑는다. 나올 수 있는 출력은 `MISSING` 7줄뿐이었다
  - `PlaceholderModelLibrary.TryInstantiateCharacter` — 캐릭터 대체의 **중간 단계**로
    TDE의 `MMExplodude` 메시를 빌려 쓰던 것. 자체 캐릭터가 몇 주째 1순위였으므로
    도달할 수 없는 코드였다
  - `Assets/CatCops/` (83파일, 1.2MB) — 레거시 프로토타입과 TDE 브리지.
    `Assets/_Project/`에서 참조가 **0건**임을 GUID로 확인하고 지웠다
  - `.gitignore`·README·`CLAUDE.md`의 "TDE를 각자 임포트하세요" 안내
  - 클립이 들어올 자리는 이제 `Assets/_Project/Art/Characters/Animations/`이고,
    `Rebuild Character Locomotion Animator`가 이름으로 찾는다 (`MODEL-002`)
  - **`AnimatorClipGuard`는 남겼다.** TDE 에셋이 아니라 우리 방어 코드이고, "쓸 수 있는
    클립이 0개면 Animator를 끈다"는 규칙은 TDE와 무관하게 필요하다 — 끄지 않으면
    휴머노이드 리타게팅이 캐릭터를 주저앉힌다 (힙 0.45m → 0.07m)

### Fixed
- **가드가 컨트롤러를 찾은 경우에만 붙고 있었다.** 컨트롤러가 늘 있던 동안은 문제가
  없었고, 위에서 그 컨트롤러를 지운 **순간** 캐릭터가 클립 0개짜리 Animator를 켠 채로
  남았다 — 절차적 보행이 Animator에게 양보하고, Animator는 재생할 것이 없고, 다리가
  하나도 안 움직인다. Play Mode 2건(`BothRolesSwingTheirLegsWhileMoving`,
  `TheLimbsTakeAJumpPoseWhileAirborne`)이 잡았다. 가드는 이제 컨트롤러 유무와 무관하게
  붙는다.

### Changed
- **WebGL 페이로드 100.27MB → 48.25MB** (`Optimize Texture And Audio Budgets`).
  먼저 실측부터: 문서의 33.9MB는 실내 19곳·보물 62개·건물 모델이 들어오기 **전**
  수치라 이미 무의미했고, 다시 재니 **100MB**였다. `wasm`은 오히려 줄었다(14.02 →
  13.36) — TDE 잔재 제거분이다. 늘어난 것은 전부 에셋이었다.
  - **원인은 적어두지 않은 예산이었다.** 삼각형 예산은 `CLAUDE.md`에 있고 지켜지고
    있었는데 텍스처 예산은 아무도 적은 적이 없다. Tripo 모델이 전부 2048²
    베이스컬러를 달고 들어와 그대로 남았고, **선반 위 금속 열쇠가 상점 실내와 같은
    2048²** 였다. 34장 × 2.7MB = 92MB. 삼각형 표와 같은 모양으로 예산을 넣었다
    (실내·건물·캐릭터 1024 / 소품·환경 512, crunch). 모델 텍스처 96.5MB → **2.8MB**
  - **메시는 전부 무손실로 깎았다.** 이 프로젝트에 **노멀맵이 0장인데**(`.fbm`에
    베이스컬러뿐) 모든 모델이 탄젠트를 임포트하고 있었다. 정적 소품에 블렌드셰이프와
    애니메이션도 켜져 있었다. `_col` 충돌 사본 8개는 씬 생성 때 렌더러가 파괴되고
    `MeshCollider`만 남는데 노멀·UV·머티리얼을 들고 있었다. 메시 45.9MB → **18.6MB**
  - **`_col`의 메시 압축은 일부러 껐다.** 위치를 양자화하는데, 그것으로 사는 실패가
    "플레이어가 통과하는 벽"이다
  - **UI 압축은 다운로드에 거의 듣지 않았다** (48.27 → 48.25MB). 무압축 RGBA는 평면
    만화 아트라 gzip이 아주 잘 먹어서 DXT+crunch로 바꿔도 압축 후 크기가 비슷하다.
    GPU 메모리는 4배 줄어드니 WebGL 메모리 한도 쪽으로 남겼다. **크기는 줄이지
    않았다** — 로비 아트는 1138×785로 이미 표시 해상도이고, 줄이면 눈에 보인다
  - 확인: 로비·결과 화면을 1920×1080으로 실제 렌더해 **압축 아티팩트 없음**을 눈으로
    봤고, 평면도로 메시 압축의 기하 손상이 없음을 봤다. Edit 299/299, Play 211/213
- **Resources 임포트 설정 정리** (`Optimize Resource Import Settings`).
  `Resources` 아래는 참조 여부와 무관하게 **무조건 빌드에 들어간다** — 코드가 런타임에
  경로로 찾는 폴더라 Unity가 미리 알 수 없기 때문이다. 그래서 과대한 소스가 디스크
  비용이 아니라 **배포 용량**이 되는 유일한 자리이고, 정확히 거기에 있었다.
  - 손으로 그린 HUD 아이콘 11종이 **1254×1254**였다. 같은 슬롯에서 같은 일을 하는
    모델 베이크 아이콘은 **192×192**다. 차이를 요구한 것은 아무것도 없었고, 기본 상한
    2048에 걸리지 않아 그대로 들어왔을 뿐이다. **256으로 제한 + 압축 + 밉맵 해제**
  - 로비 BGM이 `DecompressOnLoad` + Vorbis 품질 1이었다. 메뉴 밑에서 도는 루프 하나에
    쓰는 예산이 아니다. **CompressedInMemory + 품질 0.5**

- **대왕 반지 경보의 "도둑 4초 노출"이 한 번도 일어난 적이 없다.** `LootAlarm`이
  `thief.GetComponent<FlashlightVisibility>()`를 불렀는데 **그 컴포넌트는 경찰에게만
  있다** — 이 컴포넌트는 *보는 쪽*에 붙어서 반대쪽을 숨긴다. `?.`가 통째로 삼켰고,
  예외도 경고도 로그도 없었다.
  - **사이렌은 울리고 경찰 질주는 걸렸다.** 그래서 동작하는 경보로 보였다. 빠진 것은
    그 물건을 지킬 값어치가 있게 만드는 4초뿐이었고, 기존 테스트
    (`LiftingTheWatchedPieceSpeedsThePoliceUp`)는 **질주만 단정**하고 있었다
  - 노출은 **복제되지 않았다.** 경보는 호스트에서 울리고 노출되는 사람은 반대쪽
    기계에 있다 — 호스트만 아는 노출은 아무도 모르는 노출이다. 이제
    `NetworkItemCoordinator.RevealRole`로 나가고, 지속 시간이 메시지에 실린다
    (센서등 2.5초와 경보 4초가 다르다. 전에는 받는 쪽이 2.5초로 **넘겨짚었다**)
  - "역할 하나를 그 역할이 아닌 모두에게 보이게 한다"는 규칙이 두 벌 있었고 한 벌이
    거꾸로였다. `FlashlightVisibility.RevealRole` 하나로 합쳤다

### Changed
- **`PlayerStatusBannerView`가 `TrapStatusOverlayView`를 대체한다.** 매 프레임
  `FindObjectsByType`를 두 번 돌던 것을 **캐시**로 바꿨다 — 한 경기에 한 번 생기고
  다시 만들어지지 않는 오브젝트 둘을 보려고 WebGL 대상 빌드에서 초당 120번씩 씬을
  훑고 있었다. 역할이 바뀌면 즉시, 못 찾았으면 **초당 2번**만 다시 훑는다.
  - 캐시가 **첫 일치에서 멈추지 않는다.** 멈추면 절반만 채워진 잔여 오브젝트가 먼저
    걸렸을 때 루프가 거기서 끝나고, 배너는 기절이 달려 있지 않은 캐릭터를 경기 내내
    쳐다본다
  - 말할 수 있는 상태가 둘 늘었다: **경보 질주**(경찰, 12초 1.25배 —
    `BoostRemainingSeconds`를 **아무 UI도 읽고 있지 않았다**)와 **은신 중**(도둑).
    한 번에 하나만, 못 움직이는 것 → 보이는 것 → 보너스 순서로
- **바나나를 밟으면 별 대신 한 바퀴 돈다** (`SlipSpinView`). 바나나와 돌은 같은 1초를
  가져가면서 **같은 그림**(머리 위 별 4개)을 쓰고 있었다. 별 4개는 "누가 너를 때렸다"는
  만화 기호인데 바나나에 맞는 사람은 없다 — 밟고 넘어지는 것이고, 넘어지는 것이 이 소품의
  농담 전부다.
  - **규칙은 그대로다.** 1초도, 재기절 방지 간격도, 몰수 여부도 바뀌지 않았다. 바뀐 것은
    누가 그리는가뿐이고, 그것을 테스트로 못박았다
    (`TellingTheAccidentsApartDidNotChangeHowLongTheyLast`)
  - **플레이어 루트가 아니라 `VisualRoot`를 돌린다.** 모터가 매 프레임 루트 회전에 진행
    방향을 쓰므로 거기서 돌리면 즉시 덮어써지고, 더 나쁘게는 기절이 끝났을 때 캐릭터가
    엉뚱한 방향을 보게 된다 — 플레이어는 그것을 "입력이 씹혔다"로 읽는다
  - 회전 도중 기울었다가 **한 바퀴 안에서 다시 선다.** 세워주는 두 번째 연출이 필요 없다.
    중간에 끊겨도(경기 종료·체포·`Clear`) 720°/s로 되돌아간다 — 비뚤어진 채 남는 것이
    제일 나쁘다
  - 원인을 `StunCause`(`Impact`/`Slip`/`Stuck`)로 나누고 **복제한다**
    (`NetworkPlayerLink._stunCause`). 클라이언트는 어떤 소품이 맞았는지 듣지 않고
    "얼마 동안 붙잡혔다"만 듣는다 — 복제하지 않으면 호스트 화면은 돌고 클라 화면은 별이
    뜬다
- **실내 전리품을 호스트 시드로 뽑는다** (`LOOT-012`, `MatchDrawSeed`). 방 배치는
  `UnityEngine.Random`으로 **호스트만** 뽑고 있었다. 그림은 맞았다 —
  `NetworkLootLink`가 위치를 복제한다 — 그런데 복제되는 것은 **연출 트랜스폼**이라
  클라이언트의 `LootItem` 자체는 씬에 저장된 자리에 그대로 있었다. 두 기계가 그림 말고는
  아무것도 합의하지 못했고, 네트워크가 이미 실어 나르는 것 말고는 방이 아무것도 뽑을 수
  없었다.
  - 호스트가 **정수 하나**(`NetworkMatchMirror._matchSeed`)를 굴려 보내고 양쪽이 같은
    추첨을 돌린다. 위치를 안 받았으니 위치를 기다릴 것도 없다. 2프로세스 실측에서 이제
    **양쪽 모두 13개 방을 전부 뽑는다** (이전에는 호스트만)
  - 컨테이너 시드가 `ContainerId.GetHashCode()`였다. .NET 문자열 해시는 **프로세스마다
    무작위**라 같은 경기의 호스트와 클라이언트가 같은 찬장에서 다른 내용물을 굴렸다 —
    id가 막으려던 바로 그것이다. FNV-1a로 바꾸고 매치 시드와 섞는다
  - `LOOT-013`(종류를 자리 수보다 늘리기)은 **필요 없다**고 결론냈다. 경찰은 물건을 못
    줍고, 필요한 구분은 "훔쳐서 없음 / 아직 있음"뿐이다

### Added
- **끈끈이와 센서등이 화면에 표시된다** (`PlayerStatusBannerView`). 둘 다 만들어진 뒤로
  **아무 말도 하지 않았다.** 끈끈이는 3초를 가져가면서 돌과 같은 별 4개를 그려서 "맞았는데
  유난히 오래 간다"로 읽혔고, 센서등은 더 나빴다 — 가져가는 것이 없고 **보이게만** 하므로
  결과가 전부 상대 화면에서 일어난다. 도둑은 지나가고, 아무 변화도 못 보고, 설명되지 않은
  이유로 잡혔다.
  - 화면 **가장자리**만 물들인다. 문어와 달리 둘 다 시야를 가져가지 않으므로 가운데를
    덮으면 없던 페널티가 생긴다
  - 남은 초를 적는다. 계속 키를 누를지 3초를 받아들이고 다음을 계획할지 정할 수 있어야 한다
  - 둘이 겹치면 **끈끈이가 이긴다.** 못 움직이는 것이 보이는 것보다 위고, 배너 두 장이
    겹치면 깨진 배너 한 장으로 읽힌다
  - `InkBlindOverlayView`처럼 **스스로 설치한다.** 프리팹에 넣어야 하는 오버레이는 두 HUD
    경로 중 하나에서 빠지는 오버레이다
  - "발각됐다"는 **보는 쪽**(`FlashlightVisibility`)에 적혀 있다. 당한 사람은 물어볼 데가
    없었다 — `ViewerRole`·`RevealRemainingSeconds`를 열어 반대편에서 읽는다
- **`Report Interior Loot Models`** 에디터 메뉴. 방 물건이 씬에 있으면서 틀릴 수 있는
  경로가 셋인데 **셋 다 아무것도 로그하지 않는다**: 모델을 못 찾아 큐브로 대체됨,
  머티리얼 리맵이 비어 흰 덩어리(`ISSUE-058`), 렌더러 자체가 없음. 방마다 세므로
  "한 상점만 채워지고 열두 방은 큐브"가 총계에 숨지 않는다.
  - **실측 (2026-08-08)**: 62개 전부 자기 모델과 자기 텍스처
    (`loot_gold_bar`, `loot_watermelon`, `loot_diamond_ring` …).
    큐브 0개, 렌더러 없음 0개, 기본 머티리얼 0개. 13개 방이 전부 채워져 있다
    (감옥 제외). 렌더러에 역할 제한이 없으므로 **경찰·도둑 양쪽에 보인다** —
    줍는 것만 도둑 전용이다

### Fixed
- **빈 `allowedIntents`를 "전부 금지"로 읽고 있었다.** 월드 컨텍스트는 소켓으로 오고
  그 클라이언트는 WebGL 전용이라, 이번에 만든 **동기 경로에서는 목록이 항상 비어 있다** —
  `validateCandidates`가 모든 후보를 버려서 **명령이 전부 조용히 사라졌을 것이다.** 게임은
  아무것도 못 받고 어디에도 오류가 없다. "미지정"으로 고쳤다. 스텁 파이프라인 테스트가
  실제 전사에 닿기 전에 잡았다 (`VOICE-019`).

### Added
- **냉동 문어에 맞으면 화면에 먹물이 퍼진다** (`InkBlindOverlayView`). `BlindedState`는
  `ITEM-008`부터 동작했고 **화면에 아무 말도 하지 않았다** — 2.2초 동안 시야를 가져가는데
  화면이 그대로여서, 맞았다는 걸 아는 유일한 방법이 "내가 지금 찍고 있네"였다. 하는 일이
  "안 보이게 하는 것"뿐인 소품은 안 보이는 것이 눈에 보여야 한다.
  - 스프라이트를 **런타임에 생성**한다. 먹물 그림이 없고, 여기서 만들면 아티스트도 임포트
    설정도 필요 없고 WebGL에서 그대로 돈다. 실제 아트가 오면 `CreateSplatter` 하나만 바꾼다
  - **완전히 가리지 않는다.** 새까만 화면은 버그나 씬 로드로 읽히고, 살아 있는지도 모르는
    플레이어는 추측하는 대신 손을 놓는다 — 추측하게 만드는 게 이 소품의 값어치다
  - 얼룩이 아니라 **번진 방울 26개**다. 단일 타원은 비네트로 읽힌다 — 조명 효과지 누가
    던진 것이 아니다
  - **이 기계가 맡은 역할의** `BlindedState`만 본다. 첫 번째로 찾은 것을 쓰면 맞지 *않은*
    쪽 화면에 먹물이 낀다 — 승·패 스팅어가 승자를 따라가서 냈던 것과 같은 실수다
  - 클릭을 먹지 않는다. 가져가는 것은 시야지 조작이 아니다 `VoiceCommandBackendClient`가 동기 엔드포인트를
  쓰고 응답의 `transcript`·`classification`을 읽는다. 소켓은 **상대 플레이어에게 이벤트를
  미는 용도로만** 남는다 — 크리티컬 패스에 두면 Windows와 WebGL이 각자 다른 경로로 답을
  받게 되고, 경로는 갈라진다. 받은 결과는 `CompanionVoiceCommandBridge.ApplyBackendResult`로
  넘긴다: 불복종 굴림은 **호스트 한 곳에서** 돌아야 두 화면이 같은 결론을 본다. 새 메서드가
  기존 소켓 핸들러를 **재사용**하는 이유도 같다 — 해석·시드·디스패치·복제가 두 벌이면
  결국 동물이 말을 들었는지에 대해 서로 다른 답을 낸다. 새로운 것은 배달 방식뿐이다.
  `classification`도 `transcript`도 없으면 **실패로 처리하지 않는다** — 소켓이 아직 배달
  경로인 배포(구버전 서버, `?wait` 무시)에서는 소켓 리스너가 명령을 끝내므로, 여기서
  오류를 내면 일찍 도착한 요청을 탓하는 것이 된다.
- **키 없이 도는 스텁과 개발용 전사 주입** (`VOICE-017`). `OPENAI_API_KEY`가 없으면
  `StubSpeechToTextClient`·`StubIntentClassifierClient`로 자동 전환하고 시작 시 경고를
  남긴다 — 켜고 끄는 스위치를 두지 않은 이유는, 손으로 켜는 스텁은 켜진 채로 남고 실키가
  있는데도 켜져 있으면 돈 낸 것을 버리기 때문이다. `transcript` 필드를 보내면 받아쓰기를
  건너뛴다: **마이크도 키도 없이** `짖어`를 넣어 의도→불복종 사슬 전체를 돌리고
  `PetCognitionConfig` 밸런스를 맞출 수 있다. `VOICE_ALLOW_TRANSCRIPT_OVERRIDE`는
  **기본 꺼짐**이고 꺼져 있으면 무시가 아니라 **403**이다 — 조용히 버리면 주입이
  적용됐는데 모델이 반대한 것처럼 보인다.
- **`?wait=1` 동기 응답** (`VOICE-018`). 답이 소켓으로만 오는데 그 클라이언트가 WebGL
  전용이라 Windows 빌드는 이 백엔드를 아예 쓸 수 없었다 — 그러면 두 플랫폼이 다시 다른 AI
  스택으로 갈라지고, 그게 `VOICE-012`가 없애려던 것이다. 같은 처리를 돌린 뒤 소켓
  리스너가 받았을 **같은 레코드**를 읽어 응답한다. 경로가 둘이면 답도 둘이 된다.
- **"무조건 이해한다".** 허용 목록 클램프로 후보가 비거나 모델이 `UNKNOWN`을 내면
  결정적 매처가 마지막 말을 한다. 의심은 거절이 아니라 **낮은 확신**으로 실려 가고,
  `PetCognitionResolver`가 그것을 순종 굴림에 곱하므로 애매한 매치가 **자신 있는 오작동이
  아니라 헷갈리는 동물**이 된다 — 원하던 "낮은 확률로 엉뚱한 행동"이 여기서 나온다.

### Changed
- **음성 전송이 플랫폼이 아니라 설정으로 결정된다** (`VOICE-012`). `VoiceCommandInput`의
  상태 기계에서 `#if UNITY_WEBGL`을 걷어냈다. 이전에는 브라우저가 `server/`로, Windows가
  로컬 파이썬 게이트웨이로 갔다 — **AI 백엔드가 둘**이라 같은 말이 들고 있는 빌드에 따라
  다르게 해석될 수 있었고, Windows 플레이테스트에서 튜닝한 것이 **제출할 빌드에 도달하지
  않았다.**
  - `VoiceConfig.Transport` (`Backend` 기본 / `LocalGateway`). WebGL 이관은
    `backendBaseUrl` 교체로 끝난다
  - `BrowserVoiceCaptureProvider : IVoiceCaptureProvider` — jslib 호출을 이미 있던
    캡처 인터페이스 뒤로. **녹음만 플랫폼별이고 그 뒤는 한 경로다** (브라우저에
    `Microphone`이 없고 Windows에 `MediaRecorder`가 없는 것은 설정으로 없앨 수 없다)
  - `IsAvailable`은 브라우저에서 **항상 true**다. 마이크 유무는 브라우저만 알고
    `getUserMedia`를 불러야 답한다 — false를 돌려주면 묻기도 전에 거절하는 것이고, 진짜
    판정은 `MIC_REQUIRES_HTTPS` / `MICROPHONE_UNSUPPORTED` / 권한 거부로 각자 고칠 곳을
    이름에 담아 온다
  - 세션 토큰 검사를 `Backend`일 때만 한다. 로컬 게이트웨이는 이 기계에서 도는데
    인증할 상대가 없다
  - 명령마다 WAV를 디스크에 쓰던 것을 `preserveDebugRecordings`일 때만으로 좁혔다.
    요청은 바이트를 싣고 가므로 순수한 디스크 낭비였고, **이 경로에서 브라우저에서
    동작할 수 없는 유일한 단계**이기도 했다

### Fixed
- **음성으로는 `짖어`와 `숨어`를 시킬 수 없었다.** 매처 어휘 문제가 아니라
  **도달 불가**였다 — `CompanionCommandCatalog.FromIntent`에 `CompanionCommandId.Bark`로
  가는 인텐트 문자열이 **하나도 없었고**, `Hide`도 고양이 허용 목록으로는 닿지 않았다.
  `Ctrl+4`는 되는데 음성만 안 됐다. 그래서 **받아쓰기가 완벽해도** `None`으로 떨어져
  개가 아무것도 안 하고, 예외도 로그도 없어서 마이크 문제로 읽힌다. 여덟 개 동물 명령 중
  둘이 음성이 존재한 내내 죽어 있었다. `BARK`·`HIDE` 인텐트를 넣고 양쪽 허용 목록에
  추가했다. `CompanionCommandCatalog`에 적혀 있던 "voice will map onto the same ids
  later, which is the whole point of DEC-002"를 `VoiceCommandReachabilityTests`가 검사한다
  — 숫자키로 되는 명령이 음성 경로를 잃으면 빨개진다.

### Changed
- **한국어 명령 매칭을 자모 단위로 다시 썼다** (`server/src/voice/exact-command-matcher.ts`).
  이전 매처는 **전체 문장이 동의어와 같거나 편집거리 1**이어야 맞았다. 그래서 자연스러운
  발화가 하나도 안 맞았다 — "강아지 냄새 추적해"는 `추적`을 **정확히 포함**하는데
  Levenshtein으로는 멀다. 그리고 어휘가 안전 명령 5개뿐이라 `짖어`·`숨어`가 아예 없었다.
  세 층으로 나눴다: 안전 명령은 `match()`가 확신을 갖고 답하거나 답하지 않고(모델이
  죽어도 "멈춰"는 동작해야 하므로 추측하지 않는다), 나머지는 `suggest()`가 **문장 안에서
  스템을 찾고**, 못 찾으면 **자모 근사 부분일치**로 복구한다.
  - **초성과 종성을 같은 자음 알파벳으로** 분해한다. 유니코드는 초성 ㅁ(U+1106)과 종성
    ㅁ(U+11B7)을 다르게 주는데, 한국어 오인식은 자음이 음절 경계를 넘어가는 형태가 가장
    흔하다 — `숨어`→`스모`가 정확히 그것(ㅁ이 종성에서 초성으로 이동)이다. 코드포인트를
    그대로 쓰면 거리가 2로 나와 탈락하고, 공유 알파벳으로 쓰면 1이 되어 복구된다.
    **파이썬으로 13개 사례를 실측해서 이 결함을 찾았고**, 고친 뒤 13/13 일치·오탐 0이다.
  - 허용 거리는 스템 길이의 34%다. 고정값 1은 짧은 단어에 관대하고 긴 구절에 무용하다.
  - 근사 복구는 **확신을 낮춰서**(0.8 → 0.55) 돌려준다. 그 차이가
    `PetCognitionResolver`의 순종 굴림에 곱해지므로, 애매한 매치가 **자신 있는 오작동이
    아니라 헷갈리는 동물**이 된다 — 원하는 "낮은 확률로 엉뚱한 행동"이 여기서 자연히 나온다.
  - 동물별·`allowedIntents`별로 걸러서, 서버가 나중에 버릴 인텐트를 애초에 제안하지 않는다.
  - `lexiconFor(petType)`이 받아쓰기 편향용 어휘 목록을 낸다 (`VOICE-013` 다음 단계).
- **너구리 옆의 노란 원판을 걷어냈다** (`SaleZoneMarker` 5개). 판매 트리거의 발자국을
  땅에 그려 둔 것이었고, 남겨 둔 논거는 "테두리가 안 보이는 판매 지점은 팔지 못해 봐야
  배운다"였다. 그 논거가 이제 성립하지 않는다 — 쓰레기통 속 너구리가 마당 건너에서
  보이는 지형지물이고, `MerchantTradePresenter`가 **범위에 들어서는 순간 거래창을
  연다.** 걸어가면 상점이 열리는 것이 풀밭 위의 동그라미보다 나은 피드백이고, 완성된
  아트 옆의 원판은 레벨 에디터 흔적으로 읽힌다. 장소 이름 캡션과 같은 판단이라 같은
  스위치(`ShowGreyboxDebugMarkers`)에 걸었다 — 지우지 않았으므로 트리거 자리를 다시
  보고 싶으면 켠다. **판매 트리거 5개는 그대로다** (`Black Market Sale` 5, 원판 0).

### Fixed
- **승·패 효과음이 클릭 소리로 잘렸다.** 클립은 처음부터 문제가 아니었다 —
  `GameSoundService`와 그 `AudioSource`가 **Game 씬 안에** 있어서, 승·패 스팅어를 올린
  직후 결과 씬이 로드되면 **재생 중인 오브젝트가 파괴된다.** 씬을 나가야만 재현되므로
  씬 안에서 들어보면 정상이다. 승·패만 `PersistentOneShotAudio`(`DontDestroyOnLoad`)에서
  재생한다. `Game Audio` 오브젝트 자체를 살리지 않은 이유는 거기 `GameSoundObserver`가
  함께 붙어 있어서다 — 살아남은 낡은 관찰자가 죽은 씬 참조를 들고 있고 새 것은 싱글턴
  가드에 막힌다 (`ISSUE-052`와 같은 함정). **살아남는 오브젝트는 씬 참조를 갖지 않는다**
  (`ISSUE-070`).
- **승·패 효과음이 보는 사람이 아니라 승자를 따랐다.** `HandleMatchEnded`의 주석은
  처음부터 "로컬 역할이 정한다"고 적혀 있었는데 코드는 역할을 읽지 않고 **경찰이 이기면
  무조건 팡파르**를 울렸다. 도둑은 매 경기 져도 승리음을, 이겨도 패배음을 들었다.
  `UI-016`이 결과 화면 제목에서 고친 혼동(승자 기준 → 보는 사람 기준)과 같은 것이고
  소리만 남아 있었다. 판정을 `ResolveMatchEndSound(winner, viewer)`로 떼어내 네 조합을
  테스트로 고정했다 — 인자로 받으므로 정적 로컬 역할을 건드리지 않는다 (`ISSUE-054`).
- **길이 경고가 자기 예외를 무시했다.** `SoundBankSetup`이 2.5초 초과를 경고하면서 문장에
  "경기 종료는 예외"라고 적고 정작 승·패에도 경고했다 — 무시하도록 훈련시키는 경고다.
  그리고 이제 모든 클립의 **ID · 파일 · 길이**를 한 줄씩 찍는다. 길이는 가장 자주 틀리고
  아무도 확인하지 않는 값인데 어디에도 안 나오고 있었다.
- **Windows 빌드에서 음성 명령이 애초에 도달할 서버가 없었다.** `LocalAiConfiguration`이
  `Application.dataPath`의 부모에서 `LocalAI/`를 찾고 거기서 멈췄다. 에디터에서는 그게
  프로젝트 루트라 찾히고, **빌드에서는 exe가 있는 폴더**(`Builds/Playtest/Windows`)라
  게이트웨이 실행 파일이 존재한 적이 없다. 16개 포트를 다 훑고 `GATEWAY_NOT_READY`로
  끝나는데 흔적은 시작 시 경고 한 줄이라, 증상은 마이크 문제처럼 보였다. 이제 위로
  올라가며 `LocalAI/`를 찾고(`-localAiRoot`·`PAWS_LOCAL_AI_ROOT`로 덮어쓸 수 있다)
  찾은 경로를 한 번 로그로 남긴다. 모델이 1GB가 넘으므로 빌드마다 복사하지 않는다
  (`ISSUE-069`).
- **STT가 CUDA에서 매 요청 500을 반환했다.** `cublas64_12.dll`이 없는 기계에서
  `WhisperModel(device="cuda")`는 **생성에 성공한다** — ctranslate2가 첫 encode에서야
  DLL을 연다. 그래서 `/health`는 `device: cuda, ready: true`를 보고하고 실제 요청은
  전부 실패했고, 적재 시점 폴백은 이걸 볼 기회가 없었다. `transcribe`에서도 한 번
  폴백해 CPU로 계속 서비스한다. 실측: 한국어 TTS 5초 WAV → `강아지 냄새 추적해`
  → `TRACK`, 2.5초 (첫 요청은 CPU 재적재 포함 4.2초).
- **역할마다 붙은 음성 입력 두 개가 한 장치를 동시에 열고 있었다.** `V`가
  `FindObjectsByType`으로 전부를 켜서 경찰용과 도둑용이 같은 프레임에
  `Microphone.Start`를 불렀다. 윈도우는 장치를 한 클라이언트에게 주므로 둘 중 하나는
  **표본이 전부 0인 클립**을 받는다 — `VOICE_AUDIO_SILENT`가 간헐적이었던 이유다.
  숫자키 어댑터가 `COMP-002`부터 갖고 있던 지역성 검사를 음성에도 넣고
  (`CanCaptureLocally`), 장치 단독 소유 가드를 프로바이더에 뒀다.
- **`V` 떼기가 구현돼 있지 않았다.** `docs/INPUT_BINDINGS.md`는 처음부터
  "press/release"라고 적었지만 누름만 배선돼 있어서, 손을 떼도 녹음이 5초 상한까지
  돌았다. 짧은 명령 하나에 4초를 `RECORDING`을 보며 기다리는 건 키가 안 먹는 것으로
  읽힌다. 이제 떼면 끝나고, 0.7초 미만이면 그 지점까지 채운 뒤 끝낸다 — 톡 누르면
  실패가 아니라 짧은 명령이 된다. HUD 마이크 버튼은 두 번째 누름이 정지다.
- **실패해도 30초 쿨타임이 걸렸다.** 쿨타임을 말이 끝난 순간, 결과를 알기 **전에**
  시작하고 있었다. 그래서 무음·게이트웨이 부재 같은 실패가 키를 30초 잠갔고 다시
  눌러도 아무 일이 없었다. 성공만 쿨타임을 쓴다.
- **음성 실패 문구가 원인을 말한다.** 패널이 코드를 그대로 찍어서
  `VOICE_COMMAND_REQUEST_FAILED:Cannot connect to destination host`가 화면에 떴다 —
  로컬 서버 부재, 음소거된 마이크, 거부된 권한이 전부 "음성이 고장났다"로 읽혔고 셋은
  고치는 곳이 다르다. `MIC_RETURNED_ONLY_ZEROS`(윈도우 권한)와
  `VOICE_AUDIO_SILENT`(볼륨)를 분리했다.
- **WebGL 녹음이 조용히 멈출 수 있었다.** `SendMessage`가 없는 스코프에서 예외가
  프라미스 안에서 사라졌고, `Stop`은 C#이 인자 하나를 선언한 반면 JS는 받지 않았고,
  콜백이 오지 않으면 상태가 `Recording`에 영구히 남았다. 비보안 컨텍스트를
  `MIC_REQUIRES_HTTPS`로 따로 말한다 — `navigator.mediaDevices`는 http 주소에서
  아예 없으므로, 같은 빌드가 localhost에서는 되고 LAN 주소에서는 안 된다.
- **게이트웨이 빌드가 실행되지 않는 exe를 만들 수 있었다.** venv가 conda 위에 얹혀
  있어서 `ffi-8.dll`이 `Library\bin`에 있는데 PyInstaller는 PATH만 본다. conda가
  활성화되지 않은 셸에서 빌드하면 그 DLL을 **경고로** 넘기고 종료 코드 0을 주는데,
  나온 exe는 첫 import에서 `_ctypes`로 죽는다. 스크립트가 base prefix의 DLL 경로를
  스스로 넣고, 스테이징에 빌드해 `_ctypes.pyd`를 확인한 뒤에만 교체한다 — 이전에는
  PyInstaller를 부르기 전에 동작하는 게이트웨이를 지웠고 런타임은 gitignore라 되돌릴
  것이 없었다.

### Added
- **전리품 아이콘 26종을 그 물건의 3D 모델에서 구웠다** (`PawliceAndPurrglar > UI > Bake Loot Icons`). 전리품은 29종인데 그림은 11장이라 나머지는 **가방 칸에 글자 한 자**로 그려지고 있었다 — 치즈가 "치"다. 개수만 구석에 찍힌 칸은 미완성이 아니라 고장으로 읽히고, 첫 글자가 같은 둘은 같은 물건으로 읽힌다. 모델은 이미 있고 이미 감면돼 있으므로, 렌더한 아이콘은 **방에서 못 찾을 물건을 보여줄 수가 없다.** 손으로 그린 그림이 있으면 그쪽이 먼저다 — 렌더는 대역이지 정답이 아니다. 그래픽 모드로 구워야 한다 (`-nographics`면 투명한 사각형 26장이 나오고, 그건 글자보다 나쁘다. 완성된 것처럼 보이기 때문이다).

### Added
- **도둑이 마을의 파란 쓰레기통에 `E`로 숨는다** (`PlayerHidingSpot`·`ThiefHidingState`). 숨은 동안 움직이지 않고, 그려지지 않고, 체포되지 않는다 — 셋 다여야 한다. 안 보이는데 체포되면 경찰이 허공에 손을 뻗는 그림이고, 체포만 안 되고 보이면 버그의 그림이다. 경찰이 사람 든 통에 `E`를 하면 끌어낸다. 이것이 없으면 통을 하나 찾은 도둑이 남은 경기를 그 안에서 보내고 경찰의 절반이 끝난다. 통 네 개, 각 구역에 하나씩. 골판지 상자에는 넣지 않았다 — 그쪽은 이미 보물 은닉처라 같은 키가 두 가지를 뜻하게 된다.

### Changed
- **`E`로 보물을 한 번도 집을 수 없었다.** `LootPickupProgress`는 도둑이 **계속 조르는 프레임을 세어** 절도 시간을 물리는데, `E`는 눌린 프레임에만 전달된다 — 한 번 누르면 한 번 조르고 0.35초 뒤 취소된다. 어떤 보물도 집히지 않았고 화면에는 아무 말도 없었다. 2프로세스 회귀가 이걸 못 본 이유는 프로브가 매 프레임 요청을 보내기 때문이다(한 판 988회) — **연타는 "반복해서 조르는" 설계를 우연히 만족시키는 유일한 입력 패턴이다.** 보물을 홀드형 상호작용으로 바꿔 로컬은 기존 홀드 기계가 몰고, 세션에서는 홀드형 대상일 때만 눌린 동안 계속 보낸다 (`ISSUE-066`).
- **경찰이 보물을 집는 경로가 열려 있었다.** `CanBeginHold`가 "가방이 있는가"만 물었는데 `LootCarrier`는 두 역할이 다 들고 있다. 권한표는 스캐너가 순위를 매길 때만 참고되고 호스트는 스캐너가 본 적 없는 요청도 실행한다. 역할 검사를 물건 자신에게 넣었다.
- **상점 줄의 이름과 보유 수가 줄 바깥에** 그려져 뒤 패널 위에 얹혀 있었다. `Anchor`가 pivot을 놓는데 새 rect의 pivot은 가운데라, 폭 260을 왼쪽 66px에 중심으로 두면 −64에서 시작한다. 둘 다 줄 안으로 넣고(아이콘 오른쪽에 이름, 그 오른쪽에 `보유 N`) 계약 테스트로 고정했다 (`ISSUE-067`).
- **교환 화면이 가방 전체를 그린다.** 소품 4칸 뒤 21칸이 네 칸짜리 저장소에게 17번 칸을 묻고 있어서 늘 비어 있었다 — 가방이 찬 도둑이 열면 빈 사각형 21개를 보고 "화면이 가방을 못 읽었다"로 읽는다. 보물도 그리고, 누르면 왜 안 되는지 말한다.
- **표시한 자리가 있는 실내 열세 곳이 전부 물건을 낸다.** 다섯 곳뿐이었다 — `StockRoom`이 종류당 한 방만 채우고 나머지는 경고와 함께 비워 뒀기 때문이다. 그래서 집 여덟 채가 선반 일곱 개를 비운 채 서 있었고, 그 방은 **도둑이 이미 털고 간 방과 화면에서 똑같이 생겼다.** 경찰이 집에 들어가는 이유가 정확히 그 구분이다. 이제 방마다 자기 오브젝트를 갖는다 — 종류의 첫 방은 광장의 원본을 데려가고 나머지는 복제한다. 자리보다 세 개 적게, 겹치지 않는 종류로, 방마다 따로 뽑는다 (`ISSUE-064`).
- **경찰 화면이 지금 있는 건물에 물건이 몇 개 남았는지 말한다** (`InteriorLootTallyPresenter`). 경찰은 물건을 집을 수 없고(보물은 도둑 전용이다) 문간에서 "여기엔 없다"와 "여기엔 없어졌다"는 똑같이 생긴다. "2 / 4 남음"과 "0 / 4 — 이미 털렸다"는 다른 문장이고 그 차이가 경찰이 그 방에 들어간 이유다.
- **걷기와 점프에서 팔이 위아래로 흔들린다.** 앞뒤 스윙만 있었고 그 스윙은 **탑다운 카메라가 보는 방향의 평면에서** 일어나므로 화면에 거의 도달하지 않았다 — 벌린 자세로 고정된 것처럼 보였다. 팔을 올리고 내리는 축을 뼈마다 실측해(`MeasureLiftAxis`) 스트라이드에 맞춰 흔들고, 사인 두 개를 겹쳐 위아래 체류 시간을 어긋나게 했다. 점프는 별 모양 자세를 반 초 동안 정지 화면으로 들고 있었는데 그동안 팔이 파닥인다. 앞뒤 진폭(`armAmplitude`)은 건드리지 않았다 — 그 숫자는 예전에 팔이 다리만큼 흔들려 생긴 떨림을 피하려고 낮춘 것이다.
- 경찰 구매 창에서 **코인을 왼쪽으로 옮기고 그 오른쪽에 가격을 적는다.** 자리는 `BUY` 버튼 폭에서 역산하므로 버튼이 바뀌어도 따라간다. 계약 테스트를 붙였다 — 가격 사각형이 버튼과 겹치지 않고, 코인이 숫자 왼쪽에 있고, 글자 크기의 1.45배 이상 높이를 갖는다 (`MerchantShopRowLayoutTests`). "가격 라벨이 있는가"는 이 결함이 있는 내내 참이었다.
- **라쿤 상점이 열린 동안 왼클릭이 소모품을 쓰지 않는다.** 거래창은 일부러 게임 입력을 막지 않는데(라쿤의 자리는 경찰이 가장 서고 싶어 하는 곳이라 모달 창은 도둑을 묶어 놓는다) 그래서 `BUY`를 누르는 버튼이 아래의 투척기에도 닿았다. 이동은 그대로 두고 도구만 막는 `ToolUseSuppressed`를 따로 뒀다 (`ISSUE-065`).
- **고양이 가방이 여전히 경찰 화면에 열리던 것을 구조적으로 막았다.** 앞선 수정은 키가 호스트로 넘어가지 않게 하는 **가드**였고, 가드는 한 프레임만 져도 뚫린다 — 뚫리면 누른 사람이 아니라 **상대 화면에서** 실패하므로 누른 사람에게는 "E가 안 먹는다"로만 보인다. 이제 `TryInteract`가 아무것도 하지 않고, HUD가 그 이벤트를 구독하지 않으며, 교환 화면은 로컬 역할이 도둑이 아니면 열리지 않는다. 그리고 클라이언트에서 가방 ↔ 퀵슬롯 이동이 아이템을 복제할 수 있었다 — 가방은 그 기계의 것이고 슬롯은 호스트의 것이라, 로컬에만 적용하면 호스트의 다음 갱신이 같은 물건을 슬롯에도 되돌려 놓는다. 슬롯 쪽만 호스트에 부탁한다 (`SubmitCatBagTransferRpc`, `ISSUE-063`).
- **도둑이 보물을 21칸까지 든다.** 1개 제한이었고, 그래서 1,000골드를 모으려면 상인까지 최소 다섯 번을 오가야 했다 — 경기 대부분이 같은 길을 다시 걷는 시간이었다. 같은 종류는 한 칸에 쌓인다 (`LootBag`, `docs/03` 8.2).
- **속도 페널티를 손에 든 것이 아니라 가방에서 가장 무거운 것으로 잰다.** 손으로 재면 금괴 두 개를 담고 반지를 마지막에 집어 반지 속도로 달릴 수 있다 (`LootCarryMovementPenalty`).
- **가방 화면이 실제 소지품을 그린다.** 5번 칸부터 **맵에 있는 보물 전체**를 나열하고 우하단에 개수 대신 **가격**을 찍고 있었다. 같은 코인 일곱 개 밑에 20·10·20·10이 적혀 있었고, 인벤토리처럼 보였으므로 아무도 버그로 읽지 않았다. 칸 안의 이름·가격 텍스트도 뺐다 — 70px 사각형이 세 가지를 말하면 하나도 전달되지 않는다.
- **도둑이 고양이에게 말을 걸면 가방이 경찰 화면에 떴다.** `CatInventoryInteractable.TryInteract`는 이벤트만 던지는데 그것이 **호스트에서** 실행되므로, 경찰이 호스트일 때 두 가방이 경찰 모니터에 열렸다. 암시장과 똑같은 원인이다. "키가 아니라 화면이 답하는 상호작용"을 `IScreenAnsweredInteractable`로 표시하고, 로컬 입력 두 경로가 그 키를 넘기지 않으며 HUD가 같은 키 이벤트로 **누른 기계에** 연다. 호스트에서 하던 일이 없으므로 잃는 것도 없다 — 옮기는 것은 그다음의 클릭이고 그건 `ContainerTransfer`로 따로 간다.
- 경찰 구매 창의 **가격이 아예 보이지 않았다.** 가격이 고정 위치에 있었고 86px `BUY` 버튼이 그 위를 덮었다 — 그려지긴 했고 버튼 뒤였다. 이제 버튼 폭을 지나 오른쪽 끝에서부터 배치하고, 코인 다음에 숫자가 오도록 왼쪽 정렬해 세 줄이 코인에 맞춰 선다.
- **실내 보물 배치가 한 프레임 때문에 통째로 꺼질 수 있었다.** `LootSpotDraw`가 "내가 나눠주는 기계인가"를 묻기 **전에** `_dealt`를 세워서, 경기 시작 프레임에 플레이어 오브젝트가 아직 스폰되지 않아 답할 수 없으면 그 방은 **경기 내내 저작 시점 그대로** 남고 로그도 남지 않았다. 결정한 뒤에 세운다. 권한 조회는 찾은 뒤 캐시한다 — 매 프레임 방마다 씬 전체를 훑는 것은 WebGL 빌드에 남겨 둘 것이 아니다.
- **경찰이 암시장에 상호작용조차 되지 않던 것을 고쳤다.** 구매 창을 만들었는데도 열리지 않았고, 원인은 새 코드가 아니라 `PlayerRolePermissions`의 `Sale => role == Thief`였다 — 스캐너가 암시장을 **경찰에게는 대상으로 내놓지 않으므로** 키 입력이 작용할 것이 없고 화면은 열릴 기회조차 없었다. 증상은 "새로 만든 상호작용이 안 된다"로 가장 최근 코드를 가리키는데, 원인은 한 역할만 볼일이 있던 시절에 쓰인 권한이었다. 파는 것은 여전히 도둑만이고 그 거절은 `LootSaleZone.TryInteract`에 있다 — 그쪽은 역할을 이름으로 말할 수 있고 타입에 걸린 권한은 그럴 수 없다.
- **여섯 번째 라쿤을 지웠다.** 다섯 곳의 암시장 말고 거래 야드에 판매처가 붙지 않은 라쿤이 하나 더 있었고, 그것만 뽑기와 무관하게 **항상** 있었다. 그래서 뽑기가 진짜 시장 셋을 숨기는 동안 플레이어가 확실히 찾을 수 있는 라쿤은 그 하나였고, 그 하나는 아무것도 하지 않았다 — "시장이 게임당 하나만 나온다"가 바깥에서 보이는 모습이 정확히 그것이다.
- **암시장 다섯 곳 중 셋이 경기 시작과 함께 town에서 사라지고 있었다.** 뽑기가 자리 전체(`Black Market {label}`)를 껐다. 그래서 변화가 보이지 않는다 — 플레이어는 **풀밭에서 "여기에 시장이 생길 수도 있다"를 배울 수 없고**, 이 크기의 마을에서 둘만 열리면 "시장 하나가 옮겨 다닌다"로 읽힌다. 이제 쓰레기통은 다섯 곳에 **그대로 있고**, 뽑기는 그 안의 `Market` 자식(라쿤 + 판매처)만 켠다. 같은 뽑기를 눈에 보이게 말한 것이다.
- 거래창의 `TOTAL` 금액이 **패널 오른쪽 밖으로** 밀려 있었다. 제목과 같은 pivot 실수다 — 폭 120을 오른쪽에서 24px 안쪽에 "중심"으로 두면 오른쪽 끝을 36px 넘어간다. 금액과 코인 모두 pivot을 오른쪽으로 잡았다.
- 거래창 제목과 설명이 **창 왼쪽 밖으로 나가** 지도 위와 상인 액자에 겹쳐 있었다. `Anchor`는 **pivot**을 놓는데 새 `RectTransform`의 pivot은 가운데다 — 폭 560의 캡션을 왼쪽에서 132px에 두면 −148에서 시작한다. 요소마다 크기는 맞고 자리만 **자기 폭의 절반만큼** 틀려서, 스크린샷에서 레이아웃 취향처럼 보였다. pivot을 좌상단으로 잡았다.
- 거래창 왼쪽 그리드가 **가방을 그대로 비춘다.** 소품 4칸을 아예 그리지 않고 있었다 — 방금 닫은 가방에는 물건이 있는데 1~4번이 비어 있으면 "팔 수 없는 칸"이 아니라 **화면이 가방을 못 읽은 것**으로 읽힌다. 이제 그려 두고 흐리게 표시하며, 누르면 "소품은 너구리가 사지 않아요."가 뜬다.
- **아이콘 없는 칸이 드래그되지 않던 것을 고쳤다.** 드래그 시작 조건이 "아이콘 스프라이트가 있는가"였고 보물 정의 30여 종 중 그림이 있는 것은 6종이다. 나머지는 글자로 그려져 **집을 수조차 없었고**, 칸은 차 있는데 커서가 아무것도 들지 않으니 기능이 깨진 것으로 보였다. 칸에게 직접 묻고(`InventorySlotView.HasContent`), 그림이 없으면 색 사각형이 커서를 따라간다.
- **퀵슬롯 드래그가 되돌아가던 것을 고쳤다.** 로컬에 적용하고 호스트에도 보내고 있었는데, 클라이언트에서는 호스트의 다음 슬롯 갱신이 **예전 배치**로 먼저 도착해 아이콘이 제자리로 튀었다가 다시 움직였다. 한 번의 드래그에 두 번 튀면 실패로 읽힌다. 이제 `ToolCarrier.IsRemoteControlled`를 보고 **보내거나 적용하거나 하나만** 한다.
- 가방 제목에서 `[TAB]`을 뺐다. 재화 배지와 겹쳐서 둘 다 읽기 어려웠다 — 여는 키는 하단 `BAG [TAB]` 버튼이 이미 말해 준다.
- 고양이 절도와 은닉처 회수가 "손이 비어 있을 때"가 아니라 **"가방에 자리가 있을 때"** 열린다. 그 문장은 소지 제한이 1개였을 때 같은 뜻이었고, 지금은 이미 뭔가 든 도둑이 은닉처를 영원히 비울 수 없다는 뜻이 된다.

- `docs/16_AUDIO_PLAN.md`을 실측으로 다시 썼다. 상태 표가 "클립 0개"로 남아 있었지만 실제로는 10/10이 할당돼 있다. 그 뒤에 들어온 기능(냉동 문어·진열장·대왕 반지 경보·감옥 방·실내 8종·암시장 뽑기·열쇠)의 소리가 목록에 없어서 **추가할 94종**을 이벤트와 모델까지 붙여 정리했다. 그리고 **클립을 모으기 전에 재생 구조를 고쳐야 한다**는 것을 앞으로 옮겼다 — 2D 소스 하나에 `PlayOneShot`뿐이라 3D 위치·루프·크로스페이드·역할별 청취가 없고, 목록의 상당수는 클립을 받아도 붙을 자리가 없다.

### Added
- **라쿤 암시장 거래창**을 넣었다 (`MerchantTradePresenter`, `MERCHANT-001`~`003`). 왼쪽에 가방, 오른쪽에 판매 목록, TOTAL과 `SELL`/`SELL ALL`/`CANCEL`. 판매 범위에서 **`E`로 열고 닫는다**. 창을 여는 것은 **로컬 키 이벤트**여야 한다 — 상호작용 키는 호스트로 전달되어 호스트에서 실행되므로 판매 범위의 `TryInteract`에 창을 걸면 누른 기계가 아니라 **호스트에** 열린다. 게임 입력을 막지 않아 창이 열린 채로 걸어 나갈 수 있고, 나가면 닫힌다.
- **`E`가 더 이상 손에 든 보물을 즉시 팔지 않는다.** 두 동작을 한 키에 겹쳐 두면 상점을 *보려고* 누른 순간 고르지도 않은 보물이 가방에서 나간다. 판매 범위를 "화면이 답하는 것"으로 분류하고(`PlayerInteractionScanner.CurrentTargetIsAnsweredByAScreen`) **로컬 입력 두 경로에서만** 키를 넘기지 않는다 — `LootSaleZone.TryInteract`의 판매 규칙은 그대로 남아 테스트와 2프로세스 프로브가 직접 호출한다. 안내 문구도 "Sell carried loot"에서 "너구리 암시장 열기"로 바꿨다.
- 도둑이 아닌 역할이 암시장에서 `E`를 누르면 **"너구리는 도둑과만 거래해요"**가 뜬다. 조용한 거절은 키가 고장 난 것과 구별되지 않는다.
- **경찰도 암시장에서 `E`로 창을 연다 — 방향만 반대로, 장비를 산다.** 왼쪽이 가진 퀵슬롯 4칸, 오른쪽이 너구리의 물건이고 줄마다 `BUY`가 있다 (끈끈이 덫 60 · 센서 라이트 90 · 참치캔 40). 가격과 판정을 **새로 만들지 않았다** — 슈퍼마켓의 `PoliceSupplyCounter` 세 개가 이미 그 값을 들고 있어서 재고를 그 진열대에서 읽고, 구매는 `TryInteract`(역할·지갑·자리 순서로 거절, **물건 먼저 넘기고 돈은 나중에**)를 그대로 호출한다. 두 번째 목록은 밸런스를 두 곳에서 만지게 만든다. 살 수 없을 때는 이유를 화면에 적는다 — "골드가 부족해요" / "퀵슬롯에 자리가 없어요".
- **총 재화를 가방 헤더와 화면 우상단에 상시 표시한다** (`CurrencyBadgeView`). 도둑의 승리 조건이 숫자인데 그 숫자를 보려면 화면을 열어야 했다 — 쫓기는 중에 묻게 되는 그 질문이다. 재화는 칸을 차지하지 않는다.
- **마우스 드래그로 칸을 옮긴다** (`InventorySlotDragHandler`). 보물 칸끼리는 교환·합치기, 소품 퀵슬롯끼리는 호스트를 거쳐 교환한다(`SubmitSwapToolSlotsRpc`) — 호스트가 슬롯 상태를 복제해 되돌려 보내므로 로컬만 바꾸면 다음 갱신에 튕긴다. 서로 다른 저장소 사이의 드롭은 **이유를 화면에 띄우고** 거절한다.
- HUD 패널에 둥근 테두리를 입혔다 (`HudSpriteLibrary`, `HudPanelSkin`). 스프라이트를 런타임에 굽는데, 에디터가 프리팹을 쓸 때 붙이면 에셋이 아니라서 `null`로 저장되고 **스프라이트 없는 `Image`는 흰 사각형**이 된다. 그래서 컴포넌트가 실행 시점에 스스로 입힌다.
- 실내 다섯 곳이 전부 물건을 들었다. 방마다 **표시한 자리보다 3개 적게** 놓는다 (슈퍼마켓 7/10 · 서점 6/9 · 집 4/7 · 주얼리샵 4/7 + 진열장). 세 자리가 매판 비므로 방을 외운 플레이어에게도 빈 선반이 아무것도 알려주지 않는다.
- 전리품 13종을 추가했다 — 슈퍼마켓 3(치즈·대왕 수박·홍삼 팩), 서점 2(지갑·마법책), 집 두 곳에 4종씩. `docs/17`에는 집 표가 없어 같은 표에서 집에 있을 법한 것으로 골랐고, 집마다 고가를 정확히 하나 둔다 (`docs/03` 8.6).
- 모델 9개를 감면해 넣었다 (팝콘 981,176 → 5,000 삼각형). 치즈·지갑은 상점과 집이 모델을 공유한다.

### Fixed
- 2프로세스 회귀가 도둑을 **실내로 옮겨진 보물** 옆에 세워 60초 동안 팔지 못했다. 승자·체포·복제는 전부 정상이라 실패는 "승자 없음"까지만 말했다. 프로브가 이름 순이 아니라 **판매처에서 가까운 순**으로 고른다 (`NetworkMatchProbe.NearestSellableLoot`). `ISSUE-041`이 세 번째로 같은 모양으로 왔다.
- `HouseLayoutSceneTests`가 `house1-sausage`를 집으로 세어 "서쪽 벽을 넘은 집"으로 실패했다. 이름 부분 일치라 무엇이든 집을 자칭할 수 있다 — `LootItem`이 붙어 있으면 건물이 아닌 것으로 뺐다.

- 암시장이 다섯 자리 중 **매 경기 둘만** 열린다. 호스트가 뽑고 선택을 복제한다 — 활성 여부는 스스로 복제되지 않는다 (`BlackMarketDraw`).
- 암시장 쓰레기통 자리를 다섯 곳에 세웠다 (숲·북쪽·주얼리샵·서쪽·광장). 매판 둘만 열리게 하는 뽑기는 복제 경로가 필요해 아직 남았다.
- 세 상점의 전리품이 각자 실내로 들어가고, 경기 시작마다 방 안 자리에 랜덤으로 다시 놓인다 (`LootSpotDraw`).
- 실내 다섯 곳에 보물 자리를 표시했다 (슈퍼마켓 10 · 서점 9 · 집 7 · 주얼리샵 7). 주얼리샵의 반지와 진열장은 모델이 그려 둔 중앙 케이스 위로 옮겼다.
- 주얼리샵 배치를 정했다. 중앙 진열장에 다이아몬드 반지 고정, 나머지 7자리 중 4곳에 서로 다른 4종이 매 경기 랜덤으로 놓인다. 4종을 채우려 사파이어를 추가했다 (`docs/22` 6절).
- 전용 서버 모드를 넣었다 (`-dedicatedServer`). 아무도 플레이하지 않는 서버에서 두 캐릭터가 모두 클라이언트 소유가 된다. 리눅스 헤드리스 빌드 메뉴도 추가했다 (모듈 미설치로 산출물은 미확인).
- 전송을 WebSocket으로 바꾸고 접속 주소가 호스트명을 받는다. 브라우저는 UDP를 못 쓰므로 EC2·WebGL로 가는 전제이고, 덤으로 무료 터널을 통과해 다른 네트워크의 두 사람이 지금 테스트할 수 있다 (`docs/21_REMOTE_PLAY_AND_DEPLOY.md`).
- 로컬 AI 음성 스택을 설치했다 (faster-whisper-small + Ollama qwen3:4b-instruct, 전부 로컬, API 키 없음). 게이트웨이 `/health`와 한국어 명령 분류 8종을 실측했다. STT와 게임 내 경로는 아직 미검증.
- 진열장 열쇠를 넣었다. 서점 뒤편에서 주우면 진열장을 2.8초에 **소음 없이** 열 수 있다 — 유리를 깨면 1.1초에 30m 소음이다. 1개만 들고 쓰면 사라진다. `docs/17`이 "열쇠 아이템이 없어 보류"로 적어 둔 항목이다.
- 상점 전리품 12종을 넣었다 (`docs/17` 7절 1차 목록). 슈퍼마켓·서점·주얼리샵이 각각 4종을 들고 있고, 전리품마다 자기 모델을 쓴다 — 그전까지 모든 전리품이 보석함 하나를 등급만 바꿔 쓰고 있었다.
- 전리품 크기를 휴대 방식에서 끌어온다 (`LootDefinition.GetModelSize`). 주머니 물건이 양손 물건보다 작게 보이므로 줍기 전에 운반 비용을 알 수 있다.
- 상호작용 소품 5종(참치캔·바나나·고무닭·냉동 문어·폭죽)에 실제 모델을 붙였다. 그전까지 돌을 뺀 8종이 모두 참치캔 모델을 빌려 쓰고 설치물은 그레이박스였다 (`ITEM-MODEL-001`).
- 소품 모델을 런타임에 얻는 경로를 만들었다 (`ThrowableModelLibrary`, `Resources/Props`). 에디터의 모델 라이브러리는 애셋 데이터베이스를 읽어 빌드에서 쓸 수 없다.

- **감옥이 방이 됐다.** 체포된 도둑은 경찰서 옆 길바닥이 아니라 실제 셀
  (6.2 x 6.6m) 안에서 10초를 보낸다. 사방이 막혀 있고 문이 없다 — 시계가 문이다.
  카운트다운은 HUD가 이미 보여주고 있었다 (`유치장 N초`).
- 감옥 구금 시간 11초 → **10초** (`docs/03_GAME_RULES.md`).

- **숲·호수공원·분수 정원**을 샌드박스 서쪽과 광장 자리에 넣었다. 광장은
  `fountain plaza`에서 `fountain garden`으로 바꿨다 — 이전 모델은 네 모서리에 도로
  토막이 붙어 있어서 마을의 진짜 도로와 아무 각도로나 만났다.
- **막다른 길에 `road segment 06`.** 세트에 없던 조각이다. 그 전에는 굽은 도로가
  대신 서서 길이 화면 밖으로 이어지는 것처럼 보였다. 여는 방향이 북쪽이 아니라
  **동쪽**이다 — 나머지 넷과 같다고 가정하면 마을의 모든 막다른 길이 반대쪽으로
  막힌다.
- **나무 21개·가로등 18개를 도면대로 배치했다.** 거리마다 몇 미터 간격으로 자동
  배치하던 것을 그만뒀다 — 고르게 지루하고, "여기 셋, 저기 없음"을 말할 방법이 없다.
  좌표는 도면에서 읽고 도로·건물과 겹치면 3m 안에서 비켜 세운다.
- 나무와 가로등에 **충돌체가 없다.** 기둥에 얇은 캡슐을 두던 것을 뺐다. 보이지 않는
  충돌체는 걸리는 충돌체이고, 이제 12개가 아니라 39개다.
- `SandboxViewProbe`. 빌드된 샌드박스가 스스로 화면을 찍고 종료한다
  (`-sandboxShot <경로> -shotSeconds <초>`). 평면도는 에디터 조명으로 찍은 도면이라
  색을 판단하는 데 쓸 수 없다 (`ISSUE-060`).

### Changed

- **타일 색 맞추기를 노면·연석·표시 셋 다로 넓히고, 기준을 상대값으로 바꿨다.**
  띠 기준이 절대 밝기 상수여서, 굽기가 Unlit이 된 뒤로 **연석 보정이 어떤 타일에서도
  하나도 걸리지 않고 있었다** (`ISSUE-061`). 평균만이 아니라 편차도 맞춘다 — 여섯
  타일의 노면 중앙값은 이미 같았는데 횡단보도만 편차가 열두 배라 밝아 보였다.
  표시와 연석은 밝기가 겹쳐서(횡단보도 줄무늬 = 연석 밝기) **위치로 가른다.**
  결과: 노면 (58~60, 61, 65~66), 연석 (112, 96, 80), 표시 (178, 173, 165).
  잔디는 그대로 둔다.
- **도로 타일을 구울 때 조명을 끈다.** 각 타일을 URP Unlit으로 갈아입혀 알베도만
  찍는다. 정면 직사광은 위를 보는 면을 전부 밝기 곡선 끝에 앉히므로, 감면으로 생긴
  법선의 미세한 흔들림이 흰 잡음으로 터졌다 (`ISSUE-059`).
- **`Rebuild Map Sandbox`가 텍스처 복구를 먼저 돌린다.** 리맵이 비어 있는 모델은
  흰 덩어리로 놓이고 아무 말도 하지 않는다 (`ISSUE-058`).
- 도로 조각 4종(십자·ㄱ자·T자·막다른 길)도 **감면해서 넣는다.** 각 95만 → 4만.
  빌드에는 안 들어가지만 저장소에는 들어간다. 굽는 것이 Unlit이 된 뒤로는 결과가
  같다.

- **모델 12개 감면.** 샌드박스 씬 삼각형 306만 → 39만, 빌드 에셋 140MB → 35MB.
  표정 아이콘 넷이 각 95만 → 2,000이고 화면에서 구분되지 않는다.
- `Tools/decimate_fbx.py`. 비율이 아니라 목표 삼각형 수를 받아 Blender에서 헤드리스로
  감면한다.
- `docs/20_DECIMATION_LIST.md`. 실측 기준 감면 목록과 Blender 파일 이름.
  `Report Model Weights`가 임포트된 메시를 세고 원본 파일까지 되짚는다.
- 가로등과 나무의 기둥에 얇은 충돌체. 수관은 그대로 통과한다.
- 샌드박스 맵 사방에 외벽. 본 게임과 같은 단색 회색, 3m 높이, 통과 불가.
- `Trim Build Size` / `Report Build Size Settings`. 씬 모델에 메시 압축을, 아트
  텍스처에 1024 상한을 건다. 샌드박스 빌드 에셋 총합 **140MB → 80MB**.
- 샌드박스 도로를 정사각형 조각 5종(일자·십자·ㄱ자·횡단보도·T자)으로 교체했다.
  막다른 길은 `curved road segment`가 맡는다.
- **체포 횟수와 구금 카운트다운을 클라이언트에도 보낸다** (`UI-009`). 카운터는 HUD에
  있었지만 호스트만 도는 컴포넌트를 읽고 있어서 클라이언트는 계속 0/3이었다.
  카운트다운은 구금 중일 때만 뜬다.
- **냉동 문어** (`ITEM-008`). 던져 맞히면 2.2초 앞이 안 보인다. 기절도 몰수도 없다 —
  돌은 시간을 빼앗고 이것은 정보를 빼앗는다.
- **낙하 소음** (`ITEM-009`). 보물을 떨어뜨리면 소리가 나고, 반경은 휴대 방식에서
  나온다. 주머니 물건은 소리가 없다.
- **진열장**과 **대왕 반지 경보** (`ITEM-006`·`ITEM-007`). 가장 비싼 보물은 유리 안에
  있고, 깨는 데 1.1초와 30m짜리 소음이 든다. 들어 올리면 경찰이 12초간 1.25배 빨라지고
  도둑이 4초 드러난다. 표시는 도둑이 아니라 빈 진열대를 가리킨다.
- **고무닭**과 **폭죽** (`ITEM-003`·`ITEM-004`). 아무도 붙잡지 않고 소리만 낸다.
  고무닭은 밟으면 울고 주인이 없다. 폭죽은 2.5초 도화선 뒤 저절로 터지고 설치자도
  노출시킨다. **소리 파일은 아직 없다** — 필요한 5종은 `docs/16_AUDIO_PLAN.md` B-2.
- **소음 게시판** (`ITEM-005`). 소리 사건 하나를 호스트가 적고 양쪽 기계가 본다.
  동물은 1.6초 보러 가지만 먹이를 이기지 못한다. 바닥에 퍼지는 링으로 어디까지
  들렸는지 보인다.
- **휴대 방식 4단계** (`ITEM-001`). 주머니·한 손·양손·짐짝이 이동 속도를 100%에서
  60%까지 나눈다. 등급과는 다른 축이라 시계는 비싸면서 가볍고 장신구는 싸면서 무겁다.
- **획득 시간** (`ITEM-002`). 보물을 집는 데 크기에 따라 0.4~2.4초가 걸린다. 그 자리에
  서 있어야 하고, 움직이거나 기절하면 취소되며, 진행도는 저장되지 않는다.
- 네트워크 회귀 시나리오 `steal`과 `clash`. 도둑이 1,000골드로 이기는 경로와,
  마지막 판매가 마지막 체포와 동시에 살아 있는 경우를 2프로세스로 실측한다.
- `Repair Model Textures` / `Validate Model Textures` / `Capture Model Sheet` /
  `Report Road Pieces` / `Report Road Colours` / `Report Sandbox Cost`.

- 수색 시스템 1단계. `LootTable` ScriptableObject(가중치·개수 범위·중복 상한),
  시드 기반 추첨 `LootRoller`, 수색 대상 `SearchableContainer`
  (`IPlayerInteractable` + `IHoldInteractable` 0.6초 + `IInteractionPriority`),
  `PawliceAndPurrglar/Loot/Create Default Loot Tables`와 `Validate Loot Setup`
- 컨테이너 이동 로직을 `ContainerTransfer` 한 곳으로 모았다. 클릭 두 방향과
  F키(전부 가져오기)가 같은 "꺼내고 → 넣고 → 실패하면 되돌리기"를 쓴다 — 대량
  이동을 따로 구현하면 같은 아이템이 두 곳에 생긴다
- 고양이 가방 전용이던 좌우 교환 UI를 임의 컨테이너로 일반화(`ISlotContainer`).
  화면 제목과 그리드 제목이 열린 컨테이너 이름을 따른다
- 추첨·이동 계약 테스트 14개

### Changed

- **결과 화면의 큰 제목이 승자 기준에서 보는 사람 기준으로 바뀌었다.** 이전에는
  `경찰 승리!`/`도둑 승리!`여서 진 사람도 이긴 팀 제목을 봤다. 이제 내가 이겼으면
  `승리`, 졌으면 `패배`다. VS 밴드 그림은 여전히 누가 이겼는지로 정해지므로, 진
  사람은 상대가 환호하는 그림 위에 `패배`를 본다
- 결과 화면 목업을 4:3 두 장에서 16:9 네 장(승자 × 보는 사람)으로 교체했다
- 승패 사유 문장(`도둑을 3번 체포했습니다` 등)을 제거했다. 새 목업에 그 자리가 없다
- 결과가 없을 때 제목과 배지를 비운다. 이전에는 `대기` 배지와 안내 문구가 떴는데,
  결과 없이 무언가를 주장하는 것이 `ISSUE-050`의 정체였다
- 인벤토리·교환 UI 아이콘을 고정 여백(`size - 24px`)에서 비율(`size × 0.8`)로
  바꾸고 `preserveAspect`를 켰다. 고정 여백은 같은 아이콘을 70px 슬롯에서 66%,
  82px 슬롯에서 71%로 만들어 패널마다 크기가 달랐고, 비율 유지는 아예 꺼져 있어
  세로로 긴 아이콘이 찌그러졌다

### Fixed
- 경찰 모델의 하반신 구멍이 메워졌다. 저장소의 수리본이 게임에 안 들어와 있어 교체했다. 남은 가랑이 구멍을 메우려던 시도는 하반신 면 735개를 뒤집어 되돌렸다 (ISSUE-073).
- 숲을 잔디 높이로 내리고 통과할 수 있게 했다. 건물 경로를 타서 1.28m 떠 있고 11m 박스로 막혀 있었다 — 이제 나무 사이에 숨을 수 있다 (ISSUE-072).
- Play Mode 실패가 1건으로 줄었다. 투과 테스트는 갱신된 단정 밑에 옛 단정이 남아 있었고(ISSUE-070), 경찰 HUD 테스트는 꺼진 셀렉터에 역할을 지시하고 있었다(ISSUE-071).
- 라쿤 인사 테스트 5건이 통과한다. 앞 테스트가 로드한 도시가 살아 있어서 라쿤이 그쪽 플레이어에게 인사하고 있었다 (ISSUE-069). Play Mode 실패 8 → 3.
- 실내 카메라 테스트를 현재 설계에 맞췄다. 카메라를 벽 아래로 묶는 안은 플레이에서 반려됐고, 대신 시야를 가리는 벽을 치우는 방식(`InteriorCutawayView`)을 쓴다. 실패 9 → 8.
- 설계가 바뀐 것을 모르던 Play Mode 테스트 3건을 갱신했다 — 감옥에서 도둑은 움직일 수 있고, 임포트한 실내는 가구가 메시에 용접돼 있어 칸막이가 없다. 실내 물건 배치를 기다리는 1건은 사유를 적어 보류했다. 실패 12 → 9.
- `RockPickupScenePlayModeTests`가 통과한다. 스폰 뽑기가 테스트가 놓은 도둑을 32.7m 옮기고 있었다 (ISSUE-068).
- 보물과 돌을 새 도시의 빈 땅에 재배치했다. 보물 2개가 주얼리샵 외곽 안에 있어 닿을 수 없었고, 2개는 벽에 붙어 있었다 (ISSUE-067, TASK-PORT-002).
- 클라이언트 결과 화면에도 경과 시간·체포 수·골드가 뜬다. 승자만 씬을 넘고 요약은 넘지 않았다 (ISSUE-066).
- 클라이언트에서 동물이 덜덜거리지 않는다. 복제 위치 추종이 목표에 일찍 도착하고 다음 패킷까지 멈춰 있었다. 플레이어와 동물이 같은 `ReplicatedFollow`를 쓴다 (ISSUE-063).
- 클라이언트의 동물과 플레이어가 위아래로 진동하지 않는다. 몸통의 흔들림이 패킷으로 도착하는 위치를 프레임 간 변화량으로 재고 있었다 (ISSUE-062).

- 조각마다 아스팔트와 연석 색이 다르던 것 (`ISSUE-057`). 굽는 단계에서 타일마다 자기
  색을 재서 공통값으로 옮긴다. 아스팔트는 (60, 62, 66), 연석은 (162, 151, 137)로
  맞고 흰 노면 표시는 그대로다.
- 새 도로 타일이 얼룩으로 나오던 것 (`ISSUE-056`). 윗면을 잘라 UV를 맞추는 방식은 윗면이
  아틀라스 한 덩어리일 때만 성립하는데 새 세트는 전체에 흩어져 있다. 이제 위에서 한 번
  구운 그림을 평면에 입힌다.
- 큰 구획의 사각형이 작은 구획 셋을 삼켜서 광장과 슈퍼마켓이 엉뚱한 곳에 놓이던 것
  (`ISSUE-055`). 점을 포함하는 것 중 가장 작은 구획을 고른다.
- 헤드리스 Play Mode가 URP에서 죽던 것 (`ISSUE-054`). 미니맵 카메라가 그래픽 장치
  없이 RenderTexture를 만들려다 실패했다. 결과 파일이 안 써져서 **이전 실행 결과가
  성공처럼 보였다.**
- **머지 후 경찰이 이길 수 없던 것** (`ISSUE-051`). 체포를 두 곳에서 세어서, 네 번
  가둬도 둘로 세어지고 승자가 나오지 않았다. 집계는 화면용이고 판정은 arbiter 한 곳만
  한다.
- 먼저 실패한 테스트가 정리에 도달하지 못해 일곱 개를 넘어뜨리던 것 (`ISSUE-052`).
- 만족될 수 없던 캔버스 스케일 단언 (`ISSUE-053`). 오버레이 캔버스는 렌더된 적이
  없으면 유니티가 언제나 0으로 직렬화한다.
- `clash` 회귀가 경주 결과를 제비뽑기로 판정하던 것 (`ISSUE-049`). 정상적인 경찰 승리를
  회귀로 보고했다.
- 프로브가 유리 안의 보물 옆에 서 있던 것 (`ISSUE-050`). 이름순 첫 보물을 고르는데
  대왕 반지가 첫 번째가 됐다.
- 승리를 결정한 판매 금액이 클라이언트에 도착하지 않던 것 (`ISSUE-048`).
  지갑이 씬 언로드가 파괴하는 오브젝트에 실려 있었다. 화면에는 "820 / 1000"인
  채로 승리 화면이 넘어갔다.
- MAP-002가 참조하던 도로 모델 경로. 같은 모델을 `env_road_section.fbx`로
  들여오면서 원래 폴더를 지웠고, 씬이 열리지 않아 테스트 10개가 깨졌다.

### Added (이전)

- 동물 유인 소품 2종 (`THROW-004`). 참치캔은 고양이를, 개껌은 강아지를 부른다.
  양쪽이 서로의 동물을 건드릴 수 있는 유일한 수단
- 상점 진열대 절도 (`THROW-006`). 바나나와 개껌을 도둑만 가져간다

- 동물 표정 아이콘 4종 (`ART-016`). 느낌표·전구·하트·뱃지를 동료 머리 위에 띄워
  발견·이해·성공·거부를 화면에서 읽히게 한다. 결과 20종을 얼굴 넷으로 좁혔다

- 승리 조건 확정 (`DEC-032`). 경찰은 **체포 3회**, 도둑은 누적 판매 1,000골드,
  시간 만료는 경찰 승리. `MatchConfig.arrestsToWin`으로 조정한다
- 감옥 (`THROW-012`). 체포 1회는 경기를 끝내지 않고 경찰서에 11초 구금한 뒤
  도둑 스폰에 재배치한다. `ArrestConfig.jailSeconds` (4~20초)
- `ThiefJailState`와 `ArrestJailCoordinator`. 구금은 호스트만 판정하고 위치는
  기존 복제를 탄다

- 프로젝트 루트 `AGENTS.md`
- 프로젝트 소개와 목표 구조를 담은 `README.md`
- `docs/00_PROJECT_BRIEF.md`부터 `docs/15_KNOWN_ISSUES.md`까지의 문서 패키지
- 기능, 버그, 코드 리뷰, 리팩터링 프롬프트 템플릿
- Blender 원본과 Unity 반입 자산을 분리하는 `ArtSource/` 방향
- 경찰 모델 리깅 및 `Idle`, `Run`, `ComedyRun` 기술 스파이크 계획
- 현재 Unity 프로젝트 루트의 로컬 Git 저장소와 `main` 브랜치
- Blender, FBX, 오디오와 Unity 패키지를 위한 Git LFS 규칙
- `Assets/_Project/`, `Assets/ThirdParty/`, `ArtSource/Blender/`, `Builds/`, `Submission/` 목표 폴더 구조
- Unity `6000.5.4f1`, URP `17.5.0`, Input System `1.19.0`, Test Framework `1.7.0` 고정 환경
- Windows x86_64 우선 빌드 대상과 네트워크 기술 검증 후보
- `Bootstrap`, `Game`, `Result` 기본 씬과 빌드 순서
- 중앙화된 씬 ID, 로더, 공통 전환 버튼과 에디터 검증 도구
- 경기, 플레이어, 보물, 체포, 동물과 음성용 ScriptableObject 설정
- 필수 설정을 묶는 `DefaultGameConfigSet`, Bootstrap 초기화와 명확한 범위 오류 검증
- Match, Player, Loot, Arrest, Companion, Voice, Network 분류형 런타임 로그
- 개발·제출 빌드별 로그 레벨 설정, 동일 키 중복 억제와 원본 예외 기록
- 분리된 런타임, Edit Mode, Play Mode Assembly Definition과 기본 테스트
- Windows TECH-001 전용 씬, Input System 이동 큐브와 실행 결과 기록
- Blender 5.2 테스트 더미 생성기, 5본 Generic 리그, 단일 재질과 `Idle`·`Walk`
- 이동·충돌과 모델을 분리한 `PlayerRoot/VisualRoot` 검증 프리팹과 TECH-002 Windows 빌드
- Windows 마이크, 받아쓰기 상태, 오류 코드와 키보드 대체 입력을 표시하는 TECH-003 격리 빌드
- Netcode for GameObjects `2.13.0`과 Unity Transport `6.5.0`
- 로컬 직접 IP Host·Client 접속, 두 플레이어 위치 동기화와 종료 처리를 검증하는 NET-001 빌드
- 서버 권한 경찰·도둑 역할 배정, 역할별 시작점과 2명 제한을 검증하는 NET-002 모드
- 56×44m 순환형 회색 상자 마을과 필수 장소 7개
- 주요 장소별 복수 경로, 지붕 3개, 사다리 3개와 쓰레기통 위치 4개
- 경로 폭·물리 장애물 자동 검사와 Windows 캡슐 횡단 검증
- 읽기 전용 현재 상태 계약과 순차 전환만 허용하는 MATCH-001 상태 머신
- 경기 상태 중복·건너뛰기·역방향 전환 방지와 상태 변경 알림
- 본게임 경찰·도둑 역할, 역할별 시작점 해석과 상호작용 권한표
- Game 씬의 파란 경찰·빨간 도둑 임시 역할 표시
- Input System WASD와 CharacterController를 사용하는 공통 플레이어 이동 모터
- 경찰 추적용 3D 원근 탑다운 카메라와 경기 상태 이동 제한
- MAP 자동 횡단 프로브를 명시적 `-mapAutoQuit` 검증 실행으로 격리
- 경찰 이동 코드를 복사하지 않고 같은 모터를 사용하는 도둑 이동
- `-playerRole Police|Thief` 로컬 역할 선택과 선택 역할 카메라 추적
- Space 공통 대시, 지속시간·쿨타임·경기 상태 제한과 상태 조회
- `E` 공통 상호작용, 거리 기반 대상 선택과 역할·경기 상태 권한 검사
- Game 씬 보물·판매·사다리·일반 프로토타입 상호작용 지점과 안내 UI
- 물리·이동·상호작용·네트워크와 렌더 모델을 분리한 `PlayerVisualRoot`
- Blender 모델 교체 후에도 루트 충돌과 이동을 유지하는 VisualRoot 계약 테스트
- `MatchConfig` 기반 3초 READY 카운트다운과 중복 시작 방지
- PLAYING에서만 감소하고 0초에 ENDING으로 전환되는 4분 경기 타이머
- 시간·역할·경기 상태·상호작용 안내를 읽기 전용으로 표시하는 공통 HUD
- 씬 재진입 시 공통 HUD 중복 인스턴스를 제거하는 수명주기 보호
- READY와 경기 시작 직후 경찰·도둑 목표를 한 문장으로 표시하는 역할 안내
- `AVAILABLE`, `RESERVED`, `CARRIED`, `DROPPED`, `HIDDEN`, `SOLD` 보물 상태 머신
- 판매된 보물의 모든 후속 전환을 거부하는 종료 상태 규칙
- 안정적인 ID·표시 이름·희귀도를 가진 `LootDefinition` 데이터 에셋 3종
- `LootConfig` 단일 가격표로 관리하는 일반 200·고급 350·희귀 500 가격
- 도둑·PLAYING·빈손 조건을 모두 검사하는 `LootCarrier` 보물 획득 권한
- 획득 시 `RESERVED → CARRIED` 전환과 보물·소지자 양방향 소유 관계
- Game 씬 보석상 앞 프로토타입 보물의 실제 획득 상호작용
- 플레이어 루트의 `CarryPoint`를 따라오는 보물 `PresentationRoot`
- 운반 중 월드 충돌체 비활성화와 소지자 비활성화·파괴 시 안전한 `DROPPED` 복구
- `Q` 보물 드롭, 전방 지면 탐색과 바닥 여유 높이를 적용한 안전한 월드 배치
- 비활성 경기·바닥 없음·중복 드롭 거부와 드롭 보물 재획득
- `PlayerConfig` 기반 보물 운반 속도 0.9배 페널티와 현재 배율 조회
- 소지 변경 이벤트에 연동된 비누적 페널티 적용, 드롭·플레이어 재활성화 시 원속도 복구
- 너구리 장터의 실제 `LootSaleZone` 트리거와 도둑 전용 `ThiefLootWallet`
- 희귀도 가격표 기반 판매 정산, `SOLD` 처리, 소지·이동 페널티 해제와 승리 검사 요청
- 획득·판매 요청을 식별하는 `LootRequestId`와 성공 요청 재실행 방지
- 지갑의 요청·보물 이중 정산 원장으로 버튼 연타와 네트워크 중복 판매 차단
- 판매 금액·목표 금액·보유 보물·가격·이동 페널티·판매 가능 여부를 읽기 전용으로 표시하는 도둑 HUD
- 도둑 HUD의 빈손·운반·판매 가능·정산 완료·역할 전환 표시를 검증하는 Play Mode 테스트
- 경찰·도둑 거리와 장애물을 검사하고 진입·이탈 이벤트를 발행하는 `ArrestRangeSensor`
- 비활성·잘못된 역할·자기 자신을 체포 대상으로 인정하지 않는 ARREST-001 검증
- `PLAYING` 중 유효한 체포 감지가 유지될 때만 시간을 누적하는 `ArrestProgressController`
- `ArrestConfig`의 1.5초 체포 시간을 0~1 진행도로 변환하고 완료 값에서 중복 누적을 막는 ARREST-002 검증
- 범위·시야 상실, 참가자 비활성화, 경기 상태 변경 시 진행도를 즉시 초기화하는 ARREST-003 중단 규칙
- 실제 진행 중이었을 때만 원인별 `ProgressInterrupted` 이벤트를 한 번 발행하는 중복 방지 검증
- 체포 진행도 완료를 한 번만 확정하고 경찰 승리·결과 연출 요청을 발행하는 `ArrestCompletionController`
- 체포 완료 시 경찰 승리 요청을 한 번만 발행하는 ARREST-004 통합 검증
- 경찰의 체포 진행·중단·완료와 도둑의 위험 경고를 역할별로 표시하는 `ArrestHudPresenter`
- 게임 상태를 변경하지 않는 읽기 전용 체포 게이지와 역할 전환 표시를 검증하는 ARREST-005 테스트
- 남은 시간·도둑 판매액·체포율·도난 알림·현재 목표를 모은 역할 전용 `PoliceHudPresenter`
- 보물 운반과 판매 완료 알림을 구분하고 도둑 역할에서 숨기는 UI-002 읽기 전용 HUD 검증
- 체포·판매·시간 종료 요청을 같은 평가 시점에 모아 단일 `MatchResult`를 결정하는 `MatchResultEvaluator`
- 동시 조건을 체포, 목표 판매, 시간 종료 순으로 판정하고 이미 확정된 결과를 바꾸지 않는 MATCH-004 중재 규칙
- 확정 결과를 받아 `ENDING`으로 한 번만 전환하고 이동·대시·상호작용·보물·체포를 중지하는 `MatchEndController`
- 승리 진영·이유·판매 금액·남은 시간을 씬 간 전달하고 표시하는 MATCH-006 결과 흐름과 `ResultScreenPresenter`
- 확정 결과 후 자동 Result 씬 전환, `REMATCH`와 `MAIN MENU` 결과 화면 버튼
- 타이머·판매액·보물·플레이어 위치·체포 진행도 초기화와 중복 HUD 부재, 두 번째
  경기의 정상 종료까지 검증하는 MATCH-007 재경기 Play Mode 테스트
- `Bootstrap`, `Game`, `Result` 세 씬을 모두 포함해 결과 화면과 재경기까지
  실행할 수 있는 `Build Windows Playtest` 메뉴와 Windows 개발 빌드
- Claude Code 전용 작업 지침서 `CLAUDE.md`
- 작업자가 제공한 프롭 원본 19종을 `ArtSource/Blender/Props/`에 보관하고
  `Assets/_Project/Art/Props/`로 반입하는 FBX 내보내기 스크립트 2종
- 보물(보석 상자), 너구리 장터 좌판, 쓰레기통 4개, 사다리 3개에 실제 프롭 모델
  적용과 자산 누락 시 회색 상자로 되돌아가는 `PlaceholderModelLibrary`
- 캐릭터 모델이 없어 외부 TopDown Engine 메시를 역할 색 URP 머티리얼로
  강제 적용한 경찰·도둑 임시 시각 모델
- 후보 캐릭터 메시의 키·머리 비율·본 수를 측정해 보고하는
  `Audit Placeholder Character Models` 메뉴
- 임시 캐릭터를 만화풍 몸체로 교체하고 머리 본을 2.2배로 키운 2톤 배색,
  목표 신장 1.7m 정규화와 대기 애니메이션 적용
- 캐릭터 참고용 콘셉트 렌더 이미지 `ArtSource/Reference/Characters/`

- `IDLE`·`FOLLOW`·`MOVE_TO_TARGET`·`EXECUTE_COMMAND`·`RETURN_TO_OWNER`·
  `COOLDOWN`·`DISABLED` 일곱 상태를 가진 COMP-001 동료 공통 상태 머신
- 선언된 전환만 허용하고 어디서든 `DISABLED`로 갈 수 있으며 재경기 리셋만
  `DISABLED`를 벗어나는 규칙
- 정지 구간·재추적 거리·리쉬 복구를 가진 COMP-002 소유자 따라가기
- 키보드·버튼·음성이 공유하는 COMP-003 `CompanionCommandRequest`와
  명령 ID·입력 출처·대상 엔티티·대상 위치
- 입력에서 AI로 가는 유일한 경로인 COMP-004 `CompanionCommandDispatcher`
- 경기 상태·진영·동료 존재·쿨타임·대상 유효성을 고정 순서로 검사하고 거부 시
  쿨타임을 소비하지 않는 COMP-005 검사기
- 목표 소실·경로 실패·정지 감지 시 소유자에게 복귀하고 중복 로그를 억제하는
  COMP-006 복구
- 숫자키 `1`~`4`를 명령 요청으로 바꾸는 입력 어댑터와 Game 씬의 강아지·고양이
  에이전트, 경기 종료 시 두 동료를 정지시키는 브리지
- 이동 속도를 읽어 `Idle`과 `Run`을 섞는 `PlayerLocomotionAnimator`와
  휴머노이드 리타게팅으로 만든 달리기 애니메이터 컨트롤러
- 신규 캐릭터 5종, 건물 5종, 쓰레기통 뚜껑을 Unity로 반입하고 경찰·도둑을
  실제 모델로 교체. 건물은 회색 상자를 콜라이더로만 남기고 지붕 콜라이더와
  사다리를 실제 지붕 높이에 맞춤
- 후보 모델의 키·머리 비율·리그·클립을 측정해 보고하는 감사 메뉴 2종
- 동료에 `CharacterController`를 붙여 벽과 건물을 통과하지 않게 하고, 실제 이동
  거리로 막힘을 감지하는 정지 복구
- 흔적 기반 `DOG-003` 추적. 도둑이 남긴 시간 만료형 흔적점 중 가장 최근 것을
  따라가므로 경찰이 실제 위치를 완벽히 알지 못한다
- 정보만 만드는 `CAT-004` 교란. 도둑 위치를 바꾸지 않고 지속시간이 제한되며
  하나만 활성화되어 중복 교란을 막는다
- 사족보행 동물용 임시 절차적 이동 애니메이션 (콜라이더 루트는 건드리지 않고
  시각 자식만 위아래로 움직임)
- `UI-004` 명령 버튼, `UI-005` 쿨타임 표시, `UI-006` 한국어 결과 피드백
- 버튼과 숫자키가 같은 `CommandRequest`를 만드는 `VOICE-007` 대체 입력 경로
- `-perfProbe` 인자로 프레임·렌더러·본·머티리얼·텍스처 메모리를 측정해 JSON으로
  기록하는 `ART-005` 성능 프로브. 측정이 의미를 갖도록 VSync를 끈다
- 경찰 화면에만 보이는 교란 신호 마커와 배너. 화면 밖이면 가장자리로 붙어
  방향을 알려준다
- 동물의 실제 다리 본을 대각 보행으로 흔드는 `CompanionLegAnimator`
  (강아지·고양이 각 4개 본, 콜라이더 루트는 건드리지 않음)
- `DOG-004` 수색, `DOG-005` 경계, `DOG-006` 짖기, `CAT-003` 정찰,
  `CAT-005` 절도, `CAT-006` 숨기 명령 효과
- `LOOT-005` 보물 숨기기. 지정 은신처에서만 가능하고 `HIDDEN` 상태로 기록되며
  다시 찾을 수 있고 판매된 보물은 숨길 수 없다
- `COMP-007` 귀여운 대기 행동 4종. 명령 수행·쿨타임 중에는 실행되지 않고
  승패에 영향이 없으며 명령 실패를 만들지 않는다
- 씬 재생성 마지막에 자동 실행되는 `ART-012` 최적화 패스 (머티리얼 통합,
  정적 배칭 표시, 작은 소품 그림자 제거)
- `CAT-005` 실제 절도. 고양이가 보물을 집어 소유자에게 물고 돌아와 건넨다.
  획득은 도둑의 `LootCarrier`를 거치므로 1개 제한과 이동 페널티가 그대로
  적용되고, 판매는 의도적으로 구현하지 않는다
- 동물 보행을 좌우 교대 4박자 걸음으로 교체. 상·하단 관절 두 개를 구동하고
  체공기보다 접지기를 길게 잡아 미끄러지는 느낌을 없앴다
- `MODEL-001` 캐릭터 제작 규격 문서 `docs/16_MODEL_SPEC.md`
- `ART-002` 임포트 규격 검사 도구. 스케일·원점·리그 종류·머티리얼 수·삼각형
  수·클립 이름·URP 셰이더를 검사하고 위반을 경고한다
- `ART-003` Animator 6개 상태(`Idle`·`Walk`·`Run`·`Command`·`Win`·`Lose`)와
  게임 로직이 파라미터만 쓰도록 분리한 `CharacterAnimatorParameters`
- 확정된 결과를 읽어 승리·패배 포즈와 명령 반응만 반영하는
  `CharacterOutcomeAnimator`
- `MAP-002` 장식 건물 3채에 측정 높이 기반 Box 콜라이더 부여
- `ART-005` WebGL 빌드 용량 측정 진입점
- `MAP-003` 실제로 올라가고 내려오는 사다리 3개. 같은 키로 위아래를 모두
  처리하고, 이동 중에는 `CharacterController`를 꺼서 중력·벽 판정이 이송과
  싸워 지붕에 끼는 것을 막는다. 양쪽 역할이 모두 쓸 수 있어 지붕이 완전한
  안전지대가 되지 않는다
- `AUDIO-001` 사운드 이벤트 10종과 `GameSoundBank`
- `AUDIO-002` 규칙에서 사운드로만 흐르는 단방향 `GameSoundObserver`.
  `Play`는 반환값이 없고 예외를 던지지 않아 어떤 규칙도 사운드 성공 여부로
  분기할 수 없다
- `AUDIO-003` 배경음 믹싱과 음성 캡처 중 더킹 훅
- `UX-001` 첫 플레이 안내. 상황에 맞는 한 줄만 보여주고 45초 후 스스로 사라진다
- `UX-002` 아이콘과 텍스트를 함께 쓰는 표기 (색상 의존 제거)
- `UX-003` 쿨타임 막대와 동료의 현재 행동 표시. 명령이 안 먹는 이유를 볼 수 있다
- 상호작용 대상의 콜라이더·범위·권한을 한 번에 보고하는 진단 메뉴
- 직접 IP 로비. 내 IPv4 주소 목록을 화면에 보여주고, 접속할 IP와 포트를 입력해
  호스트로 시작하거나 상대에게 접속한다
- 서버만 쓰는 `NetworkRoleBoard`. 역할이 `NetworkVariable`로 복제되어 두 화면이
  같은 값을 읽고, 클라이언트는 교체를 요청만 할 수 있어 같은 역할을 동시에
  고르는 것이 구조적으로 불가능하다
- 세 번째 접속을 스폰 전에 승인 단계에서 거절하는 2인 제한
- 로비가 배정한 역할이 경기 씬으로 전달되는 경로 (명령줄 인자보다 우선)
- `-netLobby host|client` 인자로 접속과 역할 배정을 자동 검증하고 JSON을
  기록하는 로비 프로브
- 던지기·설치 (`THROW-001`·`003`·`005`·`007`). 돌을 던지면 1.2초 기절, 바나나를
  설치하면 밟은 쪽이 1.0초 미끄러진다. 소지 슬롯은 보물과 **별도**다 — "도둑 소지
  1개"가 확정 규칙이라 공유하면 바나나를 줍는 순간 보물을 놓친다. `F` 키 하나가
  든 것에 따라 던지기와 설치를 겸하고, HUD가 무엇을 들었는지와 기절 남은 시간을
  보여준다. 돌 줍는 지점 5곳은 임시 배치다
- 판정은 호스트만 한다. 던지기는 RPC로 호스트가 명중을 정하고, 기절은
  `NetworkVariable`로 복제되며, 설치된 덫은 명명 메시지로 양쪽이 같은 자리에
  그리고 호스트만 발동시킨다. 투사체마다 `NetworkObject`를 만들지 않았다
  (`ISSUE-016`)
- 밤 조명과 경찰 손전등. 달빛으로 바꾸고 원뿔 조명을 달았다
- `FlashlightVisibility`. 부채꼴 밖의 도둑을 렌더링하지 않는다. 세상을 어둡게
  만드는 대신 도둑만 숨겨서, 고정 카메라가 지형을 계속 보여주면서도 "저기 있었는데
  못 봤다"가 생긴다. 표시만 바꾸며 호스트 시뮬레이션은 동일하다. 체포 거리보다
  넓은 3m 안에서는 항상 보인다
- `DOG-009` 발자국 표시. `DOG-003`이 기록·추적하던 흔적을 추적 중에만, 경찰
  화면에만 그린다. 데이터는 있었고 아무도 볼 수 없었다
- `CAT-009` 정찰 결과 표시. `CAT-003`이 무엇을 찾았는지는 알려주면서 어디인지는
  버리고 있었다. 보물·경찰 방향 마커를 도둑 화면에 6초간 띄운다
- **가벽을 2m로 낮췄다.** 원본 5.6m 가벽 9장이 각도마다 카메라를 막아서, 막는 것을
  지우는 방식으로는 벽이 계속 나타났다 사라졌다. 2m는 캐릭터(1.9m)의 머리만 넘으므로
  카메라가 전부 넘겨다본다. 콜라이더도 같은 2m 박스라 보이는 것과 막는 것이 일치한다 —
  모델 가벽은 이미 문마다 조각나 있어서 박스로도 문이 막히지 않는다
- 앞면만 투과되지 않던 것을 고쳤다. 면 판정을 방 오브젝트의 **원점** 기준으로 했는데, 모델은
  현관을 포함한 실루엣으로 재중심되므로 벽이 원점보다 1.09m 뒤에 있다. 앞벽이 방 안쪽으로
  분류돼 어느 면에도 속하지 않았다 (`ISSUE-045`). 앞면 부품 25개 → 82개
- **외벽은 카메라 쪽 한 면을 통째로 지운다.** 벽 패널만 지웠더니 창틀·사이딩·셔터가 남아
  캐릭터가 창살 뒤에 있는 것처럼 보였다. 면 소속은 이름이 아니라 **위치**로 판정한다 —
  방의 측정된 내부 박스를 넘어간 부품은 그 면 것이다. 이름 목록은 모델에 부품이 추가될 때마다
  고쳐야 하고, 안 고치면 조용히 실패한다 (그게 창살이 남은 이유다)
- 면 구성을 런타임에 스스로 찾는다. 렌더러 배열을 빌드 시점에 심는 대신 숫자 두 개만
  저장한다 — 에디터가 채운 목록은 이미 로비 버튼·다리 애니메이터·센서 칸수를 잃게 했다
- **외벽은 카메라 쪽 한 면만 지운다.** 광선으로 고르면 각도마다 답이 바뀌지만, 면으로
  고르면 상태가 넷뿐이고 카메라가 충분히 돌 때까지 그대로다. 0.12 여유를 둬서 대각선에서
  두 면을 번갈아 지우지 않는다
- 실내 문틀·몰딩 렌더러를 지웠다. 2m 벽 위에 4.7m 문틀만 남으면 그게 유일한 방해물이다
- 문 통과 퇴장을 **물리 콜백 밖으로 미뤘다.** `CharacterController.Move` 안에서 순간이동
  하면 컨트롤러가 자기 계산 위치로 덮어써서, 플레이어가 방 밖 문턱에 남아 21m를 떨어졌다
- 나가는 지점을 **땅이 있는 곳까지 당긴다.** 북쪽 끝 줄 집은 앞이 경계벽이라 5.4m 앞이
  맵 밖이었다. 어느 집이 "첫 번째"인지가 인스턴스 ID 순서라 간헐적으로만 드러났다
- 점프력을 4.8 → **5.4m/s (약 1.49m)** 로 올렸다. 1.17m는 소파만 넘고 주방 하부장
  (1.85m)이나 식탁 의자(1.2m)에는 못 올라갔다
- **실내에서 나올 때는 문을 지나가면 된다.** 문틀은 실제 구멍이라 그냥 통과하면 방 밖으로
  떨어졌고, 같은 자리에서 `E`를 누르면 나가졌기 때문에 조작 문제처럼 보였다. 실내의 `E`는
  옷장·서랍장 몫으로 비워 뒀다 — 자동 문은 프롬프트도 없고 스캐너가 아예 후보로 잡지 않는다
- 들어가는 문은 그대로 `E`다. 지나가다 빨려 들어가면 모든 집 현관 앞이 함정이 된다
- **점프를 추가했다 (`좌 Shift`).** 스페이스는 대시가 이미 쓰고 있어서 비켜 갔다 —
  대시는 속도·쿨타임이 규칙에 박힌 기능이라 점프를 넣으려고 없앨 것이 아니다. 초기 속도
  4.8m/s = 약 1.17m로, 2.2배 실내의 소파(1m)에 올라갈 수 있는 높이다. 땅에서만 되고,
  공중에서 또 되면 연타로 무엇이든 올라간다
- 점프 중에는 팔다리를 크게 벌린 **별 모양 자세**를 취한다. 걷기 위에 섞어서 달리다
  뛰어도 끊기지 않는다. 호스트가 공중 여부를 복제한다 — 복제된 캐릭터는 위치를 써서
  움직이므로 자기 접지 상태가 없다
- **카메라와 플레이어 사이의 벽을 숨긴다.** 방 벽 9장이 각도마다 시야를 막는데, 틈을
  찾아 카메라를 돌리는 것은 플레이어가 할 일이 아니다. 캐릭터는 절대 숨기지 않고, 바닥과
  가구도 제외한다. 화면마다 따로 계산한다
- 회귀 프로브가 걷기를 **접지 프레임에서만** 잰다. 점프 자세가 같은 뼈를 54° 돌려서
  걷기가 완전히 죽어도 건강해 보이게 됐다
- 회귀 프로브가 캐릭터를 발 기준으로 놓고, 보물·판매처를 이름 순서로 고른다.
  피벗을 땅에 놓아 도둑이 보물에서 떨어져 나가고 있었다 (`ISSUE-041`)
- **실내가 집 모델의 실제 내부가 됐다.** 손으로 지은 그레이박스 상자 대신
  `building_house_1f_with_interior`를 2.2배로 세운다 — 화장실·침실·주방·거실·식당과
  칸막이벽, 가구 111개가 전부 그대로다. 칸막이벽은 문 구멍이 있으므로 메시 콜라이더로
  막고(방마다 10개), 가구는 부품이 아니라 **물건 단위**로 박스를 준다 (방마다 25개)
- **집 19채 전부에 실내가 있다.** 지붕 있는 집도 들어간다 — 지붕은 밖에서 내부가
  보이는지만 정하고, 방은 어차피 맵 밖에 따로 있다
- **앞문·뒷문 둘 다 출입구다.** 문이 하나면 방은 함정이었다. 들어간 문과 나오는 문은
  독립이고, 들어간 쪽 끝에서 시작한다
- 실내 귀중품을 개당 50 → **20골드**로 내렸다. 8채 × 2개 = 800이었던 것이 19채에서
  1,900이 되어 방만으로 목표 1,000을 넘겼다
- 실내 카메라 상한각을 62° → 38°로 내렸다. 새 벽 높이(5.6m)에 맞춘 값이고,
  `TheInteriorCameraStaysBelowTheWalls`가 배율·거리·각도의 관계를 단정한다
- 실내는 정적 배칭 대상에서 뺐다. 프리팹 인스턴스에 렌더러마다 플래그 override가
  쓰여서 방 하나당 68KB, 19방이면 1.3MB다. 한 번에 한 방만 보이므로 배칭 이득은 작다
- 집에 들어가면 즉시 바닥을 뚫고 떨어지던 것을 고쳤다. 문이 피벗을 바닥에 맞춰서
  캡슐 전체가 바닥 안에 파묻혔다 — 발이 바닥에 닿게 놓는다 (`ISSUE-040`)
- 집 프리팹을 풀지 않고 문을 연다. 문짝을 경첩에 모으려면 프리팹을 풀어야 했고, 풀면
  부품 186개가 씬에 실체로 기록된다. `Game.unity` **9.16MB → 3.42MB**, GameObject
  3,134개 → 446개 (`ISSUE-038`)
- 반대로 돌린 집의 문이 안쪽으로 열리던 것을 고쳤다. 경첩을 월드 X축으로 잡아서
  문짝의 반대쪽 끝에 경첩이 생겼다 (`ISSUE-039`)
- `ArtSource`의 zip 12개(16.2MB)를 풀고 지웠다. Unity는 zip을 임포트하지 못하므로
  압축된 상태로는 상점 아이템을 쓸 수 없다
- 2인 회귀에서 호스트가 승패를 기록한 뒤 2.5초 더 살아 있는다. 즉시 종료하면 복제
  상태가 한 번도 전송되기 전에 세션이 내려가서, 클라이언트가 결과를 못 받은 것으로
  기록됐다 (`ISSUE-037`)
- 실내에서 커서를 숨기고 잠근다. 그래야 낮은 감도(픽셀당 0.11°)로 천천히 돌릴 수
  있다 — 커서가 창에 갇혀 있으면 느린 회전은 화면 끝에서 끊긴다. 상하도 돌아가되
  범위를 제한했다 (14~62°): 아래로는 바닥에 박히고 위로는 벽을 넘겨다본다
- **`Esc`로 커서 해제, 좌클릭으로 재잠금.** 없으면 한 기계에서 두 창을 띄운 테스트에서
  다른 창을 만질 수 없다
- 실내 조준을 카메라 정면으로 바꿨다. 커서가 화면 중앙에 갇히면 읽어도 항상 정면이
  나온다. "보는 방향으로 던진다"는 3인칭 시점이 이미 함의하는 것이다
- 센서 방향 표시가 카메라 회전을 반영한다. 이전엔 "카메라는 안 돈다"를 전제로 월드
  방위를 그대로 썼는데, 실내 카메라는 돌기 때문에 고개를 돌리면 화살이 벽을 가리켰다
- 집 출입을 앞문으로 고쳤다 (`ISSUE-034`). 재보니 현관은 `+Z`인데 트리거와 출구가
  `−Z`, 즉 뒷문이었다. 문짝이 열리는 방향도 집 안쪽이었어서 뒤집었다
- 실내에서 다른 방이 보이던 문제 수정 (`ISSUE-035`). 카메라가 플레이어보다 6.2m
  위인데 벽이 4m라 넘겨다보고 있었다. 벽 9m, 카메라 6.5m·42°, 간격 40m
- 문을 연속으로 오가면 땅으로 꺼지던 문제 수정 (`ISSUE-036`). 순간이동이 낙하
  속도를 지우지 않아 누적됐고, 0.2m 바닥을 한 프레임에 관통했다. 속도를 지우고
  바닥을 1.5m 슬래브로 바꿨다
- 실내 귀중품을 주울 수 있다. 돌과 같은 `E` 키이고, 아이템 슬롯이 아니라 **돈이
  오른다** — 운반도 판매도 없다. 개당 50골드, 전체 16개로 800골드이고 이는 목표
  1,000골드보다 **적다**: 방만으로는 이길 수 없어서 보물 운반이 여전히 필요하다
- 타입이 `Loot`이라 경찰은 주울 수 없다. 같은 지갑 중복 방지를 써서 연타해도 한 번만
  지급된다
- 집에 들어갈 수 있다 (`MAP-008`). 앞문이 실제로 열리고, 통과하면 맵 남쪽 밖에 지어둔
  **16m 사방** 방으로 이동한다. 집 외벽은 8m라 캐릭터 4명 폭뿐이어서 안에서 추격이
  불가능했다
- 캐릭터를 줄이는 대신 방을 키웠다. 줄이면 체포 1.75m·던지기 12m·손전등 17m·센서
  3.2m가 전부 실내에서 다른 의미가 된다. 방을 키우면 규칙을 하나도 안 건드린다
- 실내에서만 자유 3인칭 시점 (우클릭 드래그로 회전). 도시는 두 플레이어가 같은
  거리를 같게 읽어야 하므로 고정 시점을 유지한다
- 실내 보물을 호스트가 무작위로 배치하고 알린다 (방마다 2개, 총 16개). 공유 시드로
  양쪽이 굴리면 한쪽에서 호출 하나만 달라도 서로 다른 방에 놓인다
- 강아지 추적이 도둑이 실내에 있으면 **그 집 문**을 가리킨다. 실제 위치는 맵 밖이라
  가리키면 경찰을 맵 밖으로 보내고, 문이 정직한 답이기도 하다
- 부채꼴 밖의 도둑이 그대로 보이던 문제 수정 (`ISSUE-032`). 기절 별 예외가
  캐릭터 전체를 면제하고 있었다 — `StunStarsView`가 플레이어 루트에 붙어 있어서
  모든 렌더러가 걸렸다. 이제 별의 링을 기준으로 검사한다
- 집 크기를 하나로 통일 (`ISSUE-033`). 구획에 맞춰 늘리던 것을 경찰서 옆 1층집
  기준 배율(0.906)로 고정했다. 그 결과 북쪽 마지막 줄이 벽을 넘어서 줄을 z=45로,
  북쪽 벽을 46 → 50으로 옮겼다. 콜라이더도 실제 결과 크기로 만든다
- 게스트 화면의 떨림 수정 (`ISSUE-030`). 복제 위치를 초당 14m로 쫓아가서
  전력질주와 정지를 반복하고 있었다. `SmoothDamp`(0.09초)로 연속적으로 따라간다.
  역할과 무관하게 게스트 쪽 문제였다
- 센서 신호 칸수가 안 변한 이유 수정 (`ISSUE-031`). 호 목록이 직렬화되지 않아
  빌드에서는 비어 있었다 — 같은 함정으로 세 번째다. 이제 런타임에 스스로 찾는다
- 센서등을 도둑에게 숨긴다. 감지기의 요점은 상대가 모르고 걸리는 것이다. 발밑에
  놓는 바나나·끈끈이는 그대로 양쪽에 보인다 — 안 보이는 덫은 덫이 아니라 불운이다
- 칸수를 거리에 비례하게 바꿨다 (5m당 1칸, 바로 앞은 1칸). 신호가 건너온 거리를
  나타내는 것이라 휴대폰 신호 강도와는 반대 방향이다
- 팔 스윙을 다리의 34%로 줄였다 (`ISSUE-029`). 팔과 다리가 같은 폭으로 흔들리고
  있었고, 다리가 거의 변형되지 않는 도둑에서는 팔이 화면의 유일한 움직임이라
  걷기가 아니라 흔들림으로 읽혔다. 사람의 팔은 다리의 3분의 1쯤 움직인다.
  수직 움직임은 측정해 보니 양쪽 모두 0.08m·방향전환 0회로 동일했다 — 진동의
  정체는 팔이었다
- 센서등 신호를 실제 호 모양으로 다시 만들었다. 빨간색, 거리에 따라 켜지는 호의
  **개수**가 달라진다 (먼 곳 4개 → 가까울수록 최대 7개). 중앙에서 바깥으로 한 개씩
  파도처럼 밝아진다. 이전 사각형 3개는 크기만 키우니 굵은 막대로 보였다
- 센서등 방향 표시가 안 뜨던 문제 수정 (`ISSUE-028`). 감지는 되고 있었고 "어디서"가
  빠져 있었다. 노출이 발생 위치를 기억하고, 노출형 설치물은 번쩍임이 끝난 뒤에
  사라진다. 부채꼴도 크게 키웠다 (가까이 2.1배)
- 플레이어 몸통 상하 움직임 되돌림. 다리가 잘 안 움직이는 캐릭터에서 지배적인
  움직임이 되어 "들썩"이 아니라 "진동"으로 읽혔다
- 클라이언트에서 두 캐릭터 다리가 멈춰 있던 문제 수정 (`ISSUE-026`). 경찰 대 도둑이
  아니라 호스트 대 클라이언트였다 (호스트 24°, 클라이언트 2.5°). 복제된 위치는
  목표에 도착한 뒤 다음 패킷까지 멈춰 있어서 프레임 이동량으로 잰 속도가 0이 된다.
  호스트가 이미 보내는 속도를 받아 쓴다
- 던진 돌이 날아간다 (`ISSUE-027`). 초당 16m, 12m에 0.75초. 손에서 떠나는 순간
  판정하지 않고 **현재 위치에서** 통로를 검사하므로 **보고 피할 수 있다.** 연출
  속도도 같은 상수를 읽는다
- 센서등 방향 표시. 경찰 화면 중앙에 와이파이 모양 부채꼴이 센서 방향으로 돌고,
  거리에 따라 크기가 변한다. 자막만으로는 어디인지 알 수 없고, 건물 뒤에서 터진
  노출은 알아채지 못한다
- 경찰 경제 (`THROW-011`). 기절이 걸린 명중은 도둑 누적액의 50%를 없애고 그중
  20%를 경찰 지갑에 넣는다. 비율이 다른 것이 핵심 — 같으면 명중이 이전이 되어
  도둑은 여전히 승리 궤도에 있으면서 자기를 잡을 장비를 대준다. 지갑은 승패를
  읽지 않고 장비만 산다
- 슈퍼마켓 계산대 2개 (끈끈이 60, 센서등 90). 임시로 뿌려둔 경찰 전용 픽업은
  제거했다 — 공짜로 줍히면 화폐에 쓸 데가 없다
- 설치물 메시지 버퍼 오버플로 수정 (`ISSUE-024`). int 3개 + Vector3 = 24바이트인데
  버퍼가 20바이트였다. 크기를 손으로 세지 않고 `GetWriteSize`로 구하고, 네 메시지
  전부를 검사하는 테스트를 넣었다
- 센서등 알림 (`SensorAlertPresenter`). 경찰 화면에만, 노출이 도는 동안만. 건물
  뒤에서 드러난 도둑은 경찰이 못 보는 노출이라 한 줄이 필요하다
- 플레이어 몸통 상하 움직임. 양쪽 역할 동일. 발끝에 맞춰 뜨고 기울며, 이 모델들은
  다리가 짧고 밤에 잘 안 보여서 다리 스윙만으로는 달리는 것이 읽히지 않는다
- 경찰 설치물 2종 (`THROW-009`). 끈끈이는 3초 고정, 센서등은 2.5초 노출이고 속도는
  전혀 늦추지 않는다. 효과를 `Hold`·`Reveal` 두 종으로 모델링해서 물건을 추가해도
  판정 코드가 늘지 않는다. 노출은 손전등 부채꼴을 무시한다 — 마커로 알려주는 대신
  도둑을 실제로 보이게 한다. 임시 획득 지점 4곳(경찰 전용)
- 설치물 시각 표현 (`THROW-010`). 그전까지 설치물은 트리거만 있는 빈 오브젝트여서
  **전부 보이지 않았다.** 설치 가능한 물건을 아직 아무도 얻을 수 없어 드러나지
  않았을 뿐이다
- 기절 별이 한 번도 안 보였던 원인 수정. 메시 감기 방향이 반대라 백페이스 컬링이
  매 프레임 전부 버리고 있었다 (앞면 방향 내적 −0.997). 크게·노랗게 바꿔도 그려지지
  않는 것을 크게 만든 것이었다. 머티리얼도 양면으로 바꿨다
- 기절 표시 강화. 별 4개, 궤도 반경 0.8m (캐릭터 폭 0.45m보다 넓게), 크기 0.62m,
  꽉 찬 노란색. 손전등 부채꼴 밖에 있어도 별은 그린다 — 어둠 속에서 던진 것이
  맞았다는 유일한 확인이고, 별이 뜨는 상대는 이미 방금 맞힌 상대다
- 소지 슬롯과 픽업 상태 복제. 줍는 동작은 원래 동작했지만 호스트만 알고 있었다.
  클라이언트 화면에서는 손이 비어 있고 돌이 그대로 놓여 있어서 기능이 고장난 것과
  구분되지 않았다. 든 것은 `NetworkVariable`로, 픽업의 가져감/복귀는 명명 메시지로
  알린다
- 던지기가 보이지 않는 트리거에 막히던 문제 수정. 맵의 모든 줍는 지점·은닉처·판매처
  ·사다리가 트리거 구체이고 Unity 레이캐스트는 기본적으로 트리거를 맞힌다. 그래서
  돌이 가장 가까운 픽업에서 멈췄고, 화면에는 아무 설명도 없었다. 자기 동물도
  엄폐물이 되고 있었다 — 발밑에 붙어 다니므로 경찰은 사실상 아무것도 던질 수 없었다.
  벽은 그대로 막는다
- 돌 5개 중 4개가 건물 안에 박혀 있던 문제 수정. 도로 격자 교차점으로 옮겼고,
  씬을 만들 때 겹침을 재서 어긋나면 LogError를 낸다
- 커서 조준 던지기. 좌클릭이 커서 방향으로 던지고 `F`는 그대로 남는다. 사거리
  12m, 명중 판정은 중심선 ±1.25m 통로다. 이동 방향과 조준이 분리돼서 도망치면서
  뒤쫓는 쪽을 맞힐 수 있다
- 던지기 연출 3종. 팔이 뒤로 갔다 앞으로 넘어가고, 돌이 실제로 날아가고, 맞은
  쪽 머리 위에서 별이 돈다. 기절이 그냥 멈춘 것과 구분되지 않아서 게임이 멈춘
  것처럼 보이던 문제를 없앤다. 팔 축은 리그에서 측정한다
- 야간 밝기 상향. 달빛 0.32 → 0.62, 환경광 약 2배. 첫 시도는 스크린샷에서만
  밤이었고 실제로는 답답했다
- 역할별 암시야. 도둑 화면이 경찰보다 밝다 (+0.42 대 +0.16). 어둠 속에서 쫓기는
  쪽에 주는 보정이고, 경찰이 볼 수 있는 것은 하나도 늘지 않는다
- 손전등 부채꼴을 바닥에 표시한다. 양쪽 다 본다 — 빔을 피해 도는 것은 경계가
  보일 때만 계획이 된다. 조명·판정·표시가 `FlashlightCone` 하나를 읽으므로
  조명이 23°로 열리고 판정은 30°이던 어긋남도 함께 사라진다
- `ISSUE-011` 해소. 일반 보물 6개(1,200골드)를 맵 전역에 배치했다. 목표
  1,000골드에 여유가 한 개라 도둑은 5개를 팔면 이기고 경찰은 두 개를 막아야
  한다. 보물이 1개뿐이라 도둑의 판매 승리가 수학적으로 불가능하던 상태를
  끝냈고, 관문 B와 `NET-010`의 남은 두 시나리오가 함께 열렸다
- 사지 스윙 축을 리그마다 측정해서 고른다. 강아지는 X축이 다리를 옆으로
  18배 더 크게 벌리고 고양이·플레이어는 반대라, 축 하나를 고정해 두었더니
  강아지가 걷는 대신 노를 젓고 있었다
- 사람용 절차적 2족 보행. 다리는 반 주기 엇갈리고 팔은 같은 쪽 다리와 반대로
  흔든다. 클립이 있으면 Animator가 이기므로 클립이 없는 환경에서만 동작한다
- 4족 전용 걸음 프로파일. 보폭 36°, 무릎 42°, 1.55걸음/초로 사람보다 크고
  느리게 걷는다. 고개 좌우 흔들기(±9°)와 몸통 좌우 기울임(±4°)을 걸음 주기에
  맞춰 더했다
- `AnimatorClipGuard`. 컨트롤러에 쓸 수 있는 클립이 없으면 Animator를 끈다.
  로코모션 클립이 TopDownEngine 파일이라 저장소에 넣을 수 없고, 그 에셋이 없는
  클론에서는 휴머노이드 리타게팅이 캐릭터를 주저앉혔다 (`ISSUE-019`)
- `NetworkMatchProbe`가 캐릭터 뼈(`Hip`, `L_Foot`)의 world Y를 기록한다.
  `Renderer.bounds`로는 이 문제가 보이지 않는다. 스킨드 메시의 bounds는 루트 본
  기준 사전 계산 박스라 주저앉은 캐릭터도 정상으로 보고한다
- 너구리 상인이 쓰레기통에 숨어 있다가 플레이어가 다가오면 뚜껑을 열고 올라와
  손을 흔든다. 클립이 아직 0개라(`MODEL-002`) 동료 보행과 같은 절차적 방식이다.
  순서를 상태로 강제한다: 뚜껑이 70% 열려야 라쿤이 나타나고 움직이며, 라쿤이
  완전히 내려가야 뚜껑이 닫힌다. 숨어 있는 동안은 위치만 낮추는 게 아니라
  렌더러를 끈다. 나타나고 사라지는 순간이 항상 열린 뚜껑 뒤에 있다
- `Capture Map Overview` 에디터 메뉴. Game 씬을 수직 상공에서 PNG로 렌더한다.
  배치 문제는 눈으로만 보이는데 플레이 카메라는 캐릭터 뒤 낮은 위치라 구역이
  비었는지 도로가 어긋났는지 보여주지 못한다. 실제로 이 도구로 아래 두 결함을
  찾았다
- 같은 네트워크의 방 목록. 호스트가 1초마다 LAN 브로드캐스트로 자기를 알리고
  클라이언트가 찾은 방을 눌러 바로 참가한다. IP를 서로 읽어 줄 필요가 없다.
  직접 IP에는 방 목록을 들고 있을 서버가 없으므로(`DEC-027`) 브로드캐스트가
  서버 없이 가능한 유일한 방법이다. IP 직접 입력도 그대로 남겼다
- 프로브의 `-netJoinMode ui|room`. 세션 API가 아니라 실제 로비 버튼과 방 항목을
  눌러 검증한다. 기존 `api` 모드만으로는 UI가 죽어 있어도 통과했다
  (`ISSUE-017`)
- 맵을 북쪽 2줄, 동쪽 2줄 확장. 56×44m에서 80×68m가 됐고 새 구역에 집 12채와
  도로가 들어갔다. 기존 구역의 좌표는 그대로라 경로·스폰·사다리가 모두 유효하다
- `NET-010` 네트워크 회귀 프로브 3종 (`-netScenario full|rematch|disconnect`).
  6개 시나리오 중 4개를 실측했다. 도둑 승리와 판매·체포 동시 발생은 보물이
  1개뿐이어서 재현할 수 없다 (`ISSUE-011`)
- `NET-009` 연결 종료 처리. 상대가 나가면 세션을 정리하고 로비로 돌아간다.
  경찰 이탈·도둑 이탈·호스트 이탈·결과 화면 이탈이 모두 같은 콜백으로 오므로
  한 경로로 처리하며, 정확히 1회만 실행되는 것을 실측했다
- `NET-008` 재경기 동기화. 클라이언트의 재경기 요청이 NGO 명명 메시지로
  호스트에 가고 호스트의 씬 로드가 양쪽을 함께 되돌린다 (`DEC-029`)
- `NET-007` 체포 판정 동기화. 진행도·완료·중단이 호스트에서 복제되고
  클라이언트는 자기 센서로 막대를 지우지 않는다. 양쪽 진행도 1.5초와 완료가
  일치했다
- `NET-006` 판매와 점수 동기화. 지갑 총액을 복제하고 중복 판매 방지는 판매가
  실제로 일어나는 호스트에 남긴다. 클라이언트의 연타 11,174회에 판매는 1회만
  기록됐고 양쪽이 같은 승자를 표시했다
- `NET-005` 보물 소유권 동기화. 보물마다 링크를 붙여 상태·위치·소유자를
  복제하고, 클라이언트는 보물 상태를 스스로 바꾸지 못한다. 동시 획득은 호스트가
  순서대로 평가하므로 두 번째 요청이 이미 들려 있는 보물을 만난다
- 상호작용(`E`)·투기(`Q`)·동료 명령(`1`~`4`)을 세션 중 호스트로 라우팅하는 경로.
  이동과 같은 규칙으로, 세션 중에는 로컬 실행 경로가 아예 없다
- `NET-004` 경기 상태와 타이머 동기화. 호스트만 시계를 돌리고 클라이언트는
  복제값을 반영하며, 두 화면의 남은 시간이 0.02초 이내로 일치한다
- `NET-003` 이동 동기화. 클라이언트 입력이 RPC로 호스트에 가고 호스트만 모터를
  돌린 뒤 좌표가 복제된다. 양쪽 좌표가 0.04m 이내로 일치하는 것을 두 프로세스
  실측으로 확인했다. 세션 중에는 로컬 키보드 입력이 모두 꺼져 한 기계가 상대
  역할을 조작할 경로가 없다
- 역할을 씬 전환에 취약한 오브젝트 대신 서버의 1회 통보와 로컬 값으로 전달
  (`ISSUE-016`)
- 세션 중 씬 로드를 NGO로 넘기는 브리지. 오프라인에서는 기존 로더가 그대로
  동작한다
- 개별 컴포넌트로 조립한 로비 화면 `LobbyCanvas.prefab`. 로고·경찰·강아지·
  도둑·고양이가 각자 앵커를 가지며 1280×720부터 4:3까지 같은 배치를 유지한다
- 목업에서 요소를 잘라내는 `Extract Lobby Art From Mockup`. 테두리부터
  플러드필로 아이보리를 지우고 연결 성분으로 이웃 조각을 걸러낸다
- 버튼·패널·입력칸·발광·발바닥·마이크 스프라이트를 코드로 그리는
  `Generate Lobby Chrome Sprites`. 9-slice border까지 설정한다
- 로비를 네 해상도로 렌더해 PNG로 남기는 `Capture Lobby Layout`
- 버튼 hover·press 연출과 비활성 표현을 담당하는 `LobbyButtonMotion`
- 역할 선택을 발광·디밍·배지로 표현하는 `LobbyRoleHighlight`
- 로비 계약 Edit Mode 테스트 12개와 실제 클릭을 발화하는 Play Mode 테스트 8개
- 개별 컴포넌트로 조립한 결과 화면 `ResultCanvas.prefab`. 승패에 따라 타이틀
  아트와 VS 일러스트가 바뀌고, 통계 카드 3개가 실제 경기 값을 표시한다
- 경기 통계를 나르는 `MatchSummary`와 `MatchResultSession.ReportSummary`.
  평가기가 결정 시점에 지갑·체포 카운터·시계를 읽어 채운다
- 결과 목업에서 요소를 잘라내는 `Extract Result Art From Mockups`
- 두 화면이 공유하는 컷아웃 코어 `MockupCutter`와 빌더 헬퍼 `UiBuildKit`
- 다시하기·로비로·종료 버튼 아이콘을 코드로 그리는 생성기 3종
- 결과 화면 계약 Edit Mode 테스트 13개
- 로비와 결과를 네 해상도로 렌더하는 `Capture Lobby Layout` /
  `Capture Result Layout`
- 역할 선택에 따라 팀 아트가 대기 포즈와 하이파이브 포즈로 바뀌는
  `LobbyCharacterView`와 `RoleSelectionState`
- 작업자가 그린 팀 페어 PNG 4장을 들여오는 `Import Lobby Pair Art`. 알파가 없는
  파일은 배경을 키잉하고, 네 장 모두 자기 불투명 경계로 잘라 프레이밍을 통일한다
- 로비 재진입 시 세션을 완전히 정리하는 `LobbySessionReset`과 회귀 테스트 2개
- 역할을 고르면 해당 팀이 1.04배로 부드럽게 커지고 고르지 않은 쪽이 어두워지는
  선택 연출. 어둡게 하는 것은 알파가 아니라 밝기다 — 알파를 내리면 캐릭터를
  통해 배경이 비친다
- 배경이 한 색 불투명이고 그 위 글자가 충분히 어두운지 함께 재는 계약 테스트
  (`BackgroundIsOneFlatColourAndTheTextOnItIsDark`)

### Fixed

- 한 판을 끝내고 로비로 돌아오면 두 번째 경기를 시작할 수 없던 문제. NGO가
  `NetworkManager`를 세션과 무관하게 활성화 시점에 `DontDestroyOnLoad`하고
  `Singleton`은 비어 있을 때만 잡기 때문에, Bootstrap을 다시 로드하면 매니저가
  둘이 되고 낡은 쪽이 싱글턴을 계속 쥔다. 새 로비가 스폰하는 모든
  `NetworkObject`는 그 낡은 싱글턴으로 폴백해
  `NetworkManagerOwner is not listening`을 냈고, 역할 보드가 없으니 역할 배정이
  안 되어 `게임 시작`이 영원히 비활성이었다. `LobbySessionReset`이 살아남은
  매니저를 종료·파기하고 씬의 매니저를 주인으로 세운다. `로비로`는 세션을 먼저
  끝내고, 역할 보드는 `InstantiateAndSpawn(networkManager)`로 매니저를 명시해
  스폰한다 (`ISSUE-052`)
- TopDownEngine이 없는 클론에서 프로젝트 전체가 컴파일되지 않던 문제.
  `Assets/CatCops/`의 레거시 브리지 한 파일이 `MoreMountains`를 참조해 모두의
  빌드를 막고 있었다. `CATCOPS_TOPDOWNENGINE` 정의로 감쌌다 (`ISSUE-019`)
- 같은 환경에서 경찰과 도둑이 땅에 묻혀 보이던 문제. 실측으로 힙 0.45m →
  0.07m까지 주저앉는 것을 확인하고, 가드 적용 후 0.48m로 복구했다
- 동물의 다리 애니메이션이 빌드에서 한 번도 동작하지 않던 문제. `Awake`가
  없어 수집 결과가 씬 로드 후 비어 있었고, `Configure`는 에디터에서만 실행된다.
  보이던 움직임은 몸통 상하 운동뿐이었다
- 몸통 상하 운동이 발 디딤과 따로 놀던 문제. 별개 타이머를 쓰고 있어 아무리
  흔들어도 걷는 것으로 읽히지 않았다. 이제 걸음 위상을 읽어 발이 닿을 때 가장
  낮고 지지기 중간에 가장 높다
- 쓰레기통 뚜껑이 열리지 않던 문제. 원인이 둘이었고 둘 다 조용히 실패했다.
  (1) 통이 연결된 프리팹 인스턴스라 Unity가 자식 재부모화를 거부한다.
  예외도 반환값도 없이 `SetParent`가 무시되어 뚜껑이 경첩에 붙지 않았다.
  `UnpackPrefabInstance`로 먼저 푼다. (2) `SceneOptimizationPass`가 뚜껑을
  `BatchingStatic`으로 표시해 결합 메시로 구워 버렸다. 라쿤은 스킨드 메시라
  제외돼서 라쿤만 움직였다. 재부모화 실패는 이제 `LogError`로 드러난다
- 주택 2채가 북쪽 골목(z=18) 위에 얹혀 길을 막고 있던 문제. 상공 렌더로
  발견했다. 어떤 경로도 z=18을 쓰지 않아(남쪽 z=-18만 웨이포인트 8개) 골목을
  새 구역 도로로 대체하고 그 자리를 블록으로 만들었다
- 경찰서가 서쪽 벽을 3m 뚫고 나가 있던 문제. 중심 x=-25에 폭 12m인데 벽이
  -28이었다. 격자 위로 옮겼다
- 클라이언트에서 `CharacterController.Move called on inactive controller`가
  매 프레임 찍혀 콘솔을 덮던 문제. 세션 중에는 링크가 컨트롤러를 끄는데
  `PlayerMovementMotor.Move`가 그것을 확인하지 않았다. `CompanionAgent`가
  이미 하던 것과 같은 검사를 추가했다
- 빌드된 로비의 버튼 6개가 전부 동작하지 않아 멀티플레이를 시작할 수조차
  없던 문제. 에디터 스크립트의 `onClick.AddListener`는 비영구 리스너라 씬
  저장 시 버려진다. 연결을 프레젠터의 `OnEnable`로 옮겼다 (`ISSUE-017`)

- 사다리에 `E`를 눌러도 아무 일도 일어나지 않던 문제. 상호작용 자체는 정상
  연결돼 있었고, 대상이 누른 횟수만 세는 `PrototypeInteractable`이라 올라가는
  기능이 없었다. 실제 등반을 구현해 교체했다 (`ISSUE-015`)
- 추격 카메라가 매 프레임 `LookAt`으로 회전해, 부드럽게 따라오는 카메라가
  목표보다 뒤처질 때 시점이 이동 방향에 따라 흔들리던 문제. 카메라 회전을
  고정 오프셋에서 유도한 단일 값으로 고정했다 (`ISSUE-013`)
- `VisualRoot`와 `PresentationRoot`가 월드 원점에 남아 플레이어와 보물의 시각
  모델이 실제 위치가 아닌 맵 중앙에 그려지던 문제 (`ISSUE-012`)

### Changed

- 로비를 목업 한 장 위에 투명 버튼을 얹는 방식에서 컴포넌트 조립으로 바꿨다.
  기존 방식은 `lobby_default.png`(1448×1086)를 `preserveAspect`로 깔고 캡션
  없는 투명 버튼을 픽셀 좌표로 고정한 것이라, 화면비가 목업과 달라지는 순간
  버튼과 그림이 어긋났다. 좌우의 검은 여백은 그 밑에 깔린 `Scene UI` 캔버스가
  비친 것이다
- 로비 텍스트와 입력칸을 레거시 `UI.Text`·`InputField`에서 TMP로 옮기고
  던파 비트비트 V2를 적용했다. 프레젠터 필드 타입만 바뀌고 판정 로직은 그대로다
- Bootstrap의 `Scene UI` 루트를 비활성화하고 카메라 clear color를 아이보리로
  바꿨다. 삭제하지 않은 것은 씬 계약이 캔버스와 이동 버튼을 요구하고 그 검사가
  비활성 오브젝트도 세기 때문이다
- 방 목록에서 장치명과 주소를 화면에서 빼고 `방 n · 참가 가능 (1/2)`로 바꿨다.
  원본 값은 Console 로그로 남는다
- 내 주소 표시를 사설 IP와 루프백으로 제한했다. 공인 주소는 같은 LAN의 상대에게
  쓸모가 없고 화면에 남길 값도 아니다
- `내 역할: 👮 경찰`의 이모지를 없앴다. 던파 비트비트에 글리프가 없어 네모로
  떴다
- 로비 갱신을 매 프레임에서 0.15초 폴링 + 값이 달라질 때만 쓰기로 바꿨다
- 로비 캐릭터를 목업에서 색상 키로 뽑은 컷아웃 4장에서 작업자가 그린 팀 페어
  아트 4장으로 바꿨다. 컷아웃은 밝기와 색조로 배경을 지운 것이라 외곽이 지저분하고
  아이보리 배경에 닿는 눈의 흰자가 먹혔다. 역할 선택 표현도 발광·디밍에서
  포즈 교체(하이파이브)로 바꿨다
- 팀 아트 rect를 그 팀이 쓸 수 있는 **가장 넓은 포즈**에 맞추고 화면 바깥쪽에
  붙였다. Preserve Aspect가 rect 안에서 가운데 정렬을 하므로, rect가 아트보다
  넓으면 하이파이브 포즈가 화면 중앙 쪽으로 밀려 방 목록과 겹쳤다
- 결과 화면을 같은 방식으로 재구현했다. 기존 구현은 목업 위 투명 버튼에 더해
  결과 라벨 4개를 `fontSize 1` + 투명 + `SetActive(false)`로 만들어 두어,
  화면의 플레이 시간·체포 수·골드가 **전부 그림이었고 실제 경기 값은 한 글자도
  표시되지 않았다** (`ISSUE-050`)
- 결과 화면 통계 카드를 목업의 4개에서 3개로 줄였다. `사용 아이템`과
  `발각 횟수`는 세는 곳이 없어, 0을 채우는 대신 칸을 뺐다
- 결과 화면 텍스트를 TMP + 던파 비트비트 V2로 옮기고 영문 대문자 표기를
  한국어 문장으로 바꿨다
- 체포 중복 제거가 `MatchResultArbiter`에서 `ArrestCompletionController`로
  옮겨갔다. 판정기는 호출마다 세야 하므로, 한 번의 체포가 두 번 세어지는 것은
  래치를 가진 호출자가 막는다. 래치는 타이머가 아니라 **석방 시점**에 풀린다
- `MatchResultArbiter.TryResolve`가 `arrestsToWin`을 받는다
- 추격 카메라를 두 번에 걸쳐 당겼다. 오프셋 `(0, 16, -14)` →
  `(0, 12.36, -10.82)`로, 화면상 크기가 처음 대비 약 29% 커진다. 각도는
  그대로다: 두 성분을 같은 비율로 줄였으므로 같은 직선 위를 움직이고 고정
  회전값도 변하지 않는다
- 캐릭터·소품 크기 조정. 강아지 1.36m, 고양이 1.20m, 너구리 1.35m,
  쓰레기통 2.08m. 통이 라쿤보다 0.73m 높아 숨는 관계가 성립한다
- 쓰레기통 재질을 양면 렌더링으로 바꿨다. 메시가 한 겹이라 기본 후면 컬링에서는
  뚜껑을 열면 안쪽 벽이 그려지지 않고 통을 관통해 바깥이 보인다
- `SceneOptimizationPass`의 정적 배칭 제외를 이름 목록이 아니라 컴포넌트
  질의로 바꿨다. `RaccoonBinGreeter`가 참조하는 transform은 구조적으로
  제외된다. 이름 목록은 오브젝트 이름이 바뀌면 소리 없이 깨진다
- 확장 구역 도로를 규칙적인 격자로 다시 깔았다. 블록마다 집을 2채씩 넣어
  17채가 6열 5행으로 들어간다. 이전에는 14m 블록에 집이 하나뿐이라 빈 땅에
  건물을 떨어뜨린 것처럼 보였다. 동쪽 세로 도로는 2개에서 1개로 줄였다.
  두 번째가 기존 x=24 골목과 1m 간격이라 어떤 블록도 못 쓰는 자투리를
  만들고 있었다
- 세션 중 승패는 복제하지 않고 판정의 입력(지갑 총액, 체포 완료, 남은 시간)만
  복제한다. 판정은 여전히 양쪽의 `MatchResultArbiter`가 하므로 규칙 계층의
  단일 판정 원칙이 유지된다 (`DEC-028`)
- `LootStateMachine`에 권한 추종용 `ResetTo`를 추가했다. 중간 패킷을 놓친
  기계가 목적지 상태로 맞출 수 있게 하되, 전이 표는 그대로 유지된다
- 체포 중단이 횟수와 사유를 함께 남긴다. 값이 다시 0이 된 것과 새로 중단된
  것을 복제된 화면에서 구분하기 위한 것이다
- 체포 완료와 타이머 만료는 중앙 승패 판정기에 요청하고, 확정 결과만 경기 종료 컨트롤러가 `ENDING`으로 전환한다.
- Game 씬의 임시 결과 바로가기 버튼을 제거하고 정상 경기 종료를 통해서만 Result 씬을 연다.

- 운반 페널티 복원은 씬 초기화 순서와 무관하게 적용되도록 이동 모터의 불필요한 경기 상태 검사를 제거
- 중앙 광장 상호작용 마커를 측정 경로 밖으로 옮기고 자동 측정 중 플레이어 충돌을 격리해 MAP-001 횡단을 복구
- 프로젝트 임시 제목을 `멍경찰과 냥도둑`으로 사용
- 판매 NPC를 `너구리 상인`으로 확정
- 핵심 시점을 3D 기울어진 탑다운으로 확정
- 실제 AI와 음성 입력 전에 숫자키 `1`~`4`로 동물 명령을 검증하도록 개발 순서 변경
- 대부분의 최종 모델링을 그레이박스 프로토타입 이후로 이동
- Unity 생성 파일과 테스트 결과를 제외하도록 `.gitignore` 보강
- 사용하지 않는 2D 편집, Collaborate, Rider, Visual Scripting, Multiplayer Center 패키지 제거
- 남아 있던 `DefaultCompany`와 이전 실험 제품명을 `PawliceAndPurrglar`으로 수정
- Unity `6000.5`와 맞지 않는 NGO `2.7.0` 대신 컴파일과 실행 검증을 통과한 `2.13.0` 채택
- 네트워크 기술 검증 역할을 Host 경찰, 첫 Client 도둑으로 고정하고 추가 접속을 거절
- `Game` 씬을 MAP-001 회색 상자 마을로 교체하고 너구리 거래장터 앵커를 사용
- 백로그 `UI-007`의 잘못된 결과 화면 표기를 역할별 목표 안내로 수정

### Deprecated

- `Assets/CatCops/`의 기존 자동 생성 프로토타입을 새 MVP 구현 기준에서 제외
- 고정 암시장, 비밀 장터, 까마귀 상인 설정

### Known

- Game 씬의 보물 1개 200골드로는 목표 1,000골드에 도달할 수 없어 도둑의 판매
  승리 경로가 없음 (`ISSUE-011`)
- 로컬 1인 조작만 가능해 경찰 대 도둑의 실제 추격은 미검증
- 기존 경찰 모델은 리그와 애니메이션이 없음
- 네트워크 서비스·권한 구조와 실제 음성 기술은 미정
- 현재 개발 PC에서 Unity 내장 Windows 받아쓰기가 `0x80004003`으로 생성되지 않음
- 기본 씬 버튼의 직접 Play Mode 클릭 검증은 아직 수행하지 않음
- 숨김 창 Windows 실행에서는 MAP 자동 캡처가 렌더 프레임을 얻지 못해 시간 초과될 수 있음
