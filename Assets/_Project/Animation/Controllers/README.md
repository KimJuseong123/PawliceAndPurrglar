# Animation Controllers

Production controllers are created here only after valid authored Idle, Walk and Run clips pass the Animation Setup validation.

The current repository intentionally contains no generated or empty controller. `CharacterLocomotion.controller` remains a Legacy controller and is not overwritten by the setup tool.
