# AGENTS.md

## 1. 프로젝트 개요

- 제목: 멍경찰과 냥도둑 (확정)
- 영문명: Pawlice and Purrglar
- 장르: 1대1 캐주얼 비대칭 추격 게임
- 시점: 원근감 있는 3D 기울어진 탑다운
- 경기 시간: 4분
- 엔진: Unity
- 현재 목표: 공모전 제출용 MVP의 핵심 재미를 검증하는 프로토타입 완성

경찰 플레이어는 강아지에게 추적과 경계를 명령해 보물을 지킨다.
도둑 플레이어는 고양이에게 정찰과 교란을 명령해 보물을 훔친 뒤 너구리 상인에게 판매한다.

현재는 실제 음성 인식과 고도화된 AI를 구현하지 않는다.
모든 동물 명령은 먼저 `1`, `2`, `3`, `4` 단축키로 구현하고, 이후 같은 명령 ID에 음성 입력을 연결한다.

대부분의 캐릭터, 동물, 건물, 소품 모델은 그레이박스 프로토타입 이후 Blender에서 별도로 제작해 교체한다.
예외적으로 현재 경찰 모델 한 체만 먼저 적용해 리깅, 애니메이션, Unity 임포트 파이프라인을 검증한다.

## 2. 확정된 제품 방향

### 한 줄 소개

경찰은 강아지에게 추적과 경계를 명령하고, 도둑은 고양이에게 정찰과 교란을 명령해 4분 동안 보물을 지키거나 훔치는 1대1 음성 명령 비대칭 추격 게임.

### 핵심 경험

1. 플레이어 캐릭터를 직접 조작한다.
2. 동시에 동물 파트너에게 제한된 명령을 내린다.
3. 골목, 건물, 장애물, 투척물을 이용해 추격과 도주 경로를 바꾼다.
4. 경찰은 보물을 지키고 도둑을 체포한다.
5. 도둑은 보물을 훔쳐 너구리 상인에게 판매한다.

### 시각 및 애니메이션 방향

- 정적인 아이소메트릭이 아니라 캐릭터를 따라가는 원근 투영 탑다운 카메라를 사용한다.
- 전체 추격 상황, 동물 파트너, 투척물의 흐름이 한 화면에서 읽혀야 한다.
- 캐릭터는 친근하고 둥글며 약간 허술하고 코믹한 인상을 유지한다.
- 조작은 즉각적이어야 하며, 멍청함은 입력 지연이 아니라 애니메이션의 관성과 과장으로 표현한다.
- 추격 중 팔을 양옆으로 벌리는 달리기, 짧은 다리의 빠른 스텝, 모자와 눈알의 후속 흔들림을 사용할 수 있다.
- 피격, 미끄러짐, 급정지에는 짧은 히트 스톱과 명확한 후속 동작을 사용한다.

### 세계관 용어

- 판매 NPC는 `너구리 상인`이다.
- 기존 문서나 실험 코드의 고정 `암시장`, `비밀 장터`, `까마귀 상인`은 현재 설정이 아니다.
- 새 구현과 문서에서 폐기된 용어를 다시 사용하지 않는다.

## 3. 작업 전 확인 순서

작업을 시작하기 전에 존재하는 문서만 다음 순서로 읽는다.

1. `README.md`
2. `docs/13_CURRENT_STATE.md`
3. `docs/14_DECISION_LOG.md`
4. `docs/02_MVP_SCOPE.md`
5. `docs/03_GAME_RULES.md`
6. `docs/05_TECH_ARCHITECTURE.md`
7. `docs/09_TASK_BACKLOG.md`
8. 현재 작업과 직접 관련된 문서

문서가 아직 없으면 임의로 내용을 만들었다고 가정하지 말고, 현재 요청에 필요한 최소 범위만 진행한다.

충돌 시 우선순위는 다음과 같다.

1. 사용자가 현재 작업에서 직접 지시한 내용
2. `docs/14_DECISION_LOG.md`의 채택된 결정
3. `docs/03_GAME_RULES.md`
4. `docs/02_MVP_SCOPE.md`
5. `docs/05_TECH_ARCHITECTURE.md`
6. `docs/13_CURRENT_STATE.md`
7. 기존 코드와 기타 문서

## 4. 현재 단계의 범위

### 지금 구현해도 되는 것

- 이동, 카메라, 충돌, 상호작용의 그레이박스
- 4분 타이머와 최소 승패 판정
- 보물 획득, 운반, 판매의 최소 흐름
- 체포의 최소 흐름
- 강아지와 고양이의 명령별 상태 머신
- 숫자키 `1`~`4`를 통한 동물 명령
- 명령 이름, 상태, 쿨타임을 보여주는 임시 UI
- 경찰 모델 한 체의 리깅 및 애니메이션 검증
- 임시 도형과 단색 머티리얼을 이용한 맵 제작

### 지금 구현하지 않는 것

- 실제 STT 또는 음성 인식 서비스
- 자연어 명령 분류
- 런타임 LLM 호출
- 자유 대화형 동물 AI
- 최종 캐릭터, 동물, 건물 모델링
- 최종 애니메이션과 시네마틱
- 최종 UI, 사운드, 이펙트
- 네트워크 패키지가 확정되지 않은 상태의 본격 멀티플레이
- 계정, 매칭, 랭킹, 성장, 상점, 여러 맵

범위가 바뀌면 구현 전에 `docs/02_MVP_SCOPE.md`와 `docs/14_DECISION_LOG.md`를 갱신한다.

## 5. 저장소 경계

### 프로젝트 코드와 에셋

- 프로젝트 전용 Unity 자산은 `Assets/_Project/` 아래에 둔다.
- 외부 에셋은 `Assets/ThirdParty/` 아래에 격리한다.
- 이미 별도 루트에 설치된 외부 에셋은 직접 수정하지 않는다.
- TopDown Engine을 유지한다면 엔진 파일을 수정하지 않고 `Assets/_Project/Scripts/Integration/`의 어댑터를 사용한다.

### Blender 원본과 Unity 반입 파일

- Blender 원본은 `ArtSource/Blender/`에서 관리한다.
- Unity에는 검증된 FBX와 필요한 텍스처만 `Assets/_Project/Art/`로 반입한다.
- `.blend` 파일을 런타임 에셋의 직접 참조로 사용하지 않는다.
- 모델 교체 시 기존 프리팹의 컴포넌트와 연결을 가능한 한 유지한다.
- 모델 원점, 축, 단위, 이름 규칙을 변경하면 임포트 테스트를 다시 수행한다.

### 생성 파일

다음 경로는 직접 수정하거나 커밋하지 않는다.

- `Library/`
- `Temp/`
- `Logs/`
- `Obj/`
- `.vs/`
- `UserSettings/`
- 자동 생성된 IDE 프로젝트 파일
- 로컬 캐시

`Builds/`에는 공유하거나 제출할 빌드만 둔다.

Unity 에셋을 추가하거나 이동할 때 `.meta` 파일을 함께 유지한다.

## 6. 프로토타입 아키텍처

핵심 규칙과 표현 계층을 분리한다.

- `Core`: 경기 상태, 타이머, 승패, 공통 인터페이스
- `Gameplay`: 이동, 상호작용, 보물, 판매, 체포, 투척
- `Companions`: 강아지와 고양이의 상태 및 명령 실행
- `Input`: 키보드, 버튼, 향후 음성 입력 어댑터
- `Animation`: 애니메이터 파라미터와 표현 이벤트
- `UI`: HUD, 명령 피드백, 결과 화면
- `Integration`: 외부 에셋과 네트워크 패키지 어댑터
- `Tests`: Edit Mode 및 Play Mode 테스트

다음 규칙을 지킨다.

- 승패를 애니메이션 이벤트가 결정하지 않는다.
- UI 텍스트에서 게임 값을 읽지 않는다.
- 입력 코드가 동물 AI 상태를 직접 변경하지 않는다.
- 키보드와 향후 음성 입력은 동일한 `CompanionCommandId`를 생성한다.
- 동물 상태 머신은 자연어 문자열을 비교하지 않는다.
- 설정값을 씬 곳곳의 매직 넘버로 흩어놓지 않는다.
- 네트워크 코드를 핵심 게임 규칙에 직접 결합하지 않는다.

## 7. 단축키 기반 AI 대체 규칙

프로토타입 단계의 `1`~`4` 입력은 최종 음성 명령의 임시 대체 수단이다.

경찰 강아지의 초기 명령 후보:

- `1`: `TRACK`
- `2`: `SEARCH`
- `3`: `GUARD`
- `4`: `BARK`

도둑 고양이의 초기 명령 후보:

- `1`: `SCOUT`
- `2`: `DISTRACT`
- `3`: `STEAL`
- `4`: `HIDE`

명령 목록은 아직 밸런스 검증 전이다.
추가하거나 의미를 변경할 때는 `docs/04_AI_VOICE_SPEC.md`와 `docs/14_DECISION_LOG.md`에 기록한다.

각 명령은 다음 단계를 거친다.

1. 입력 어댑터가 명령 ID를 생성한다.
2. 실행 가능 여부와 쿨타임을 검사한다.
3. 동물 상태 머신에 명령을 전달한다.
4. UI에 접수, 실행, 실패 상태를 표시한다.
5. 애니메이션과 사운드는 결과를 표현한다.

## 8. 경찰 리깅 및 애니메이션 스파이크

기존 경찰 모델은 최종 품질 기준이 아니라 파이프라인 검증용이다.

검증 목표:

- Blender 원점과 Unity 축이 올바른가
- 모델 크기와 피벗이 게임 플레이에 적합한가
- Humanoid 리깅과 애니메이션 리타게팅이 가능한가
- 실패 시 Generic 리그가 필요한 이유가 명확한가
- 이동 코드와 애니메이션이 분리되어 있는가
- 루트 모션을 끈 상태에서 이동과 발 움직임이 자연스러운가
- 탑다운 카메라에서 실루엣과 상태가 읽히는가

초기 테스트 클립:

- `Idle`
- `Run`
- `ComedyRun`
- `Throw`
- `Hit`
- `Slip`

한 번에 전부 제작하지 않는다.
먼저 `Idle`, `Run`, `ComedyRun`으로 리그와 Animator 전환을 검증한다.

## 9. 작업 원칙

- 한 요청에서는 하나의 독립 작업을 끝낸다.
- 요청을 만족하는 가장 작은 범위만 변경한다.
- 작업과 무관한 파일을 수정하지 않는다.
- 요청받지 않은 구조 변경이나 리팩터링을 하지 않는다.
- 문서에 없는 승리 조건, 능력, 명령, 상점, 외부 의존성을 임의로 추가하지 않는다.
- 대규모 씬 또는 프리팹 YAML을 수작업으로 재작성하지 않는다.
- 씬 연결이 필요하면 정확한 경로와 연결 절차를 보고한다.
- 기존 사용자 변경을 되돌리지 않는다.
- 외부 패키지를 추가하기 전에 목적과 대안을 문서화하고 사용자 승인을 받는다.

## 10. 검증과 완료 조건

변경 범위에 맞춰 다음을 검증한다.

1. 컴파일 오류
2. 관련 Edit Mode 테스트
3. 관련 Play Mode 테스트
4. 메인 플레이 흐름 회귀
5. 경고와 예외 로그
6. 모델의 축, 크기, 머티리얼, 리그, 애니메이션

정적 검사와 Unity 런타임 검증을 구분한다.
실행하지 않은 테스트를 통과했다고 표현하지 않는다.
Unity가 다른 프로세스에서 열려 있거나 잠금 파일이 있으면 그 상태를 먼저 확인한다.

작업 완료 보고에는 다음 내용을 포함한다.

### 변경 요약

구현 또는 수정한 결과를 간결하게 설명한다.

### 변경 파일

파일별 변경 목적을 적는다.

### 검증

실행한 테스트와 결과를 적는다.

### Unity 에디터 작업

수동 연결이 필요하면 정확한 순서와 경로를 적는다.

### 남은 위험

미검증 항목과 다음 작업에 영향을 주는 위험을 적는다.

### 문서 변경

업데이트한 문서와 결정 사항을 적는다.

## 11. 금지 사항

- 요청 없이 프로젝트 전체를 다시 생성하거나 대량 삭제하지 않는다.
- 요청 없이 패키지를 설치하지 않는다.
- 비밀 키나 토큰을 저장소에 기록하지 않는다.
- 오류를 빈 `catch`로 숨기지 않는다.
- 테스트를 삭제하거나 약화해 통과시키지 않는다.
- 임시 단축키 코드를 최종 음성 AI라고 표현하지 않는다.
- 임시 모델을 최종 아트라고 표현하지 않는다.
- 폐기된 고정 암시장, 비밀 장터 또는 까마귀 상인 설정을 다시 추가하지 않는다.
- 캐릭터의 코믹함을 입력 지연이나 제어 불능으로 구현하지 않는다.
- 문서에 없는 기능을 좋은 아이디어라는 이유만으로 추가하지 않는다.
